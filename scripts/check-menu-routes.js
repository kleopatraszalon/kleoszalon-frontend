const fs=require('fs');
const path=require('path');

const app=fs.readFileSync('src/App.tsx','utf8');
const routeAccess=fs.readFileSync('src/routing/routeAccess.tsx','utf8');
const routeAggregator=fs.readFileSync('src/routing/routes.ts','utf8');
const fontScale=fs.readFileSync('src/styles/vir-font-scale.css','utf8');
const fontScaleRuntime=fs.readFileSync('src/utils/fontScale.ts','utf8');
const fontScaleControl=fs.readFileSync('src/components/FontScaleControl.tsx','utf8');
const languageSwitcher=fs.readFileSync('src/components/LanguageSwitcher.tsx','utf8');
const indexSource=fs.readFileSync('src/index.tsx','utf8');
const appLayout=fs.readFileSync('src/layouts/AppLayout.tsx','utf8');
const appTopbar=fs.readFileSync('src/components/AppTopbar.tsx','utf8');
const sessionIdleGuard=fs.readFileSync('src/hooks/useSessionIdleGuard.ts','utf8');
const appLayoutModel=fs.readFileSync('src/layouts/appLayoutModel.ts','utf8');
const appLayoutCss=fs.readFileSync('src/layouts/AppLayout.css','utf8');
const sidebar=fs.readFileSync('src/components/Sidebar.tsx','utf8');
const accountingSidebar=fs.existsSync('src/components/AccountingSidebar.tsx')?fs.readFileSync('src/components/AccountingSidebar.tsx','utf8'):'';
const staticMenus=sidebar+'\n'+accountingSidebar;
const routeFiles=['src/routing/publicRoutes.tsx','src/routing/bookingRoutes.tsx','src/routing/hrRoutes.tsx','src/routing/financeRoutes.tsx','src/routing/inventoryRoutes.tsx','src/routing/adminRoutes.tsx'];
const routeSources=routeFiles.map(file=>({file,source:fs.readFileSync(file,'utf8')}));
const routes=routeSources.map(entry=>entry.source).join('\n');
const critical=['/appointments/calendar','/workorders','/modules/team/timetable','/modules/team/attendance','/modules/customers/clients','/modules/customers/crm','/employees','/loyalty','/finance','/warehouse','/warehouse/products','/warehouse/operations','/warehouse/lots','/warehouse/central-supply','/masterdata/services','/reports/top-metrics','/knowledge-base/checklists','/webshop/admin','/admin/access-control','/signage','/signage/appearance','/kiosk','/hr/positions'];
const scopedMenuRoutes=['/appointments/calendar','/workorders','/modules/team/timetable','/knowledge-base/checklists','/finance','/warehouse','/warehouse/products','/warehouse/operations','/warehouse/lots'];
const failures=[];const escapeRegex=value=>value.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');const hasRoute=route=>new RegExp(`path\\s*:\\s*["']${escapeRegex(route)}["']`).test(routes);const hasDynamicCustomerRoute=()=>/path\s*:\s*["']\/modules\/customers\/:view["']/.test(routes);
for(const route of critical){const exact=hasRoute(route);const customerDynamic=route.startsWith('/modules/customers/')&&hasDynamicCustomerRoute();if(!exact&&!customerDynamic)failures.push(`A routerben hiányzik: ${route}`)}for(const route of scopedMenuRoutes)if(!staticMenus.includes(route))failures.push(`A szerepkör-alapú statikus menüben hiányzik: ${route}`);
const routeOwners=new Map();for(const {file,source} of routeSources){for(const match of source.matchAll(/\bpath\s*:\s*["']([^"']+)["']/g)){const route=match[1];const owners=routeOwners.get(route)||[];owners.push(file);routeOwners.set(route,owners)}}for(const [route,owners] of routeOwners)if(owners.length>1)failures.push(`Duplikált router útvonal (${owners.length}x): ${route} -> ${owners.join(', ')}`);const routeMatches=[...routeOwners.keys()];if(routeMatches.length!==187)failures.push(`A moduláris router route-száma megváltozott: várt 187, aktuális ${routeMatches.length}. Ellenőrizd, hogy route nem veszett-e el.`);
if(!app.includes('from "./routing/routes"'))failures.push('Az App.tsx nem a routing/routes aggregátort használja.');if(/\bpath\s*:\s*["']/.test(app))failures.push('Route deklaráció került vissza az App.tsx-be; tedd a megfelelő domain route modulba.');if(/\blazy\s*\(/.test(app))failures.push('Lazy page-regisztráció került vissza az App.tsx-be; használd a routing/routePages modult.');for(const moduleName of ['publicRoutes','bookingRoutes','hrRoutes','financeRoutes','inventoryRoutes','adminRoutes'])if(!routeAggregator.includes(moduleName))failures.push(`A route aggregátorból hiányzik: ${moduleName}`);if(/createBrowserRouter\s*\(/.test(routes))failures.push('Domain route modul nem hozhat létre saját BrowserRouter példányt.');if(!/export\s+function\s+RequireAuth/.test(routeAccess)||!/export\s+function\s+RequireRoles/.test(routeAccess))failures.push('A központi routeAccess modulból hiányzik az auth vagy role guard.');if(!/ADMIN_ROLES/.test(routeAccess)||!/MANAGEMENT_ROLES/.test(routeAccess)||!/KIOSK_MANAGER_ROLES/.test(routeAccess))failures.push('A route szerepkör-csoportok nincsenek központilag definiálva.');if(!app.includes('./styles/vir-font-scale.css'))failures.push('A globális VIR typography scale nincs betöltve az App.tsx-ben.');if(!/--vir-font-scale\s*:\s*100%/.test(fontScale)||!/font-size\s*:\s*var\(--vir-font-scale\)/.test(fontScale))failures.push('A VIR typography 100%-os alapértéke vagy root alkalmazása hiányzik.');if(!/DEFAULT_FONT_SCALE\s*=\s*100/.test(fontScaleRuntime))failures.push('A VIR alapértelmezett betűmérete nem 100%.');if(!fontScaleRuntime.includes('FONT_SCALE_OPTIONS = [100, 125, 150, 175, 200]'))failures.push('A támogatott betűméret lépcsők megváltoztak vagy hiányoznak.');if(!fontScaleRuntime.includes('kleo.font.scale.v1')||!fontScaleRuntime.includes('localStorage.setItem'))failures.push('A betűméret-beállítás tartós tárolása hiányzik.');if(!fontScaleControl.includes('A−')||!fontScaleControl.includes('A+')||!fontScaleControl.includes('saveFontScale'))failures.push('A felső betűméret-vezérlő nem tartalmazza a csökkentés/növelés funkciót.');if(!languageSwitcher.includes('FontScaleControl')||languageSwitcher.indexOf('FontScaleControl')>languageSwitcher.lastIndexOf('</label>'))failures.push('A betűméret-vezérlő nincs közvetlenül a nyelvválasztó mellett.');if(!indexSource.includes('initializeFontScale();'))failures.push('A mentett betűméret nem áll vissza az alkalmazás renderelése előtt.');if(!appLayout.includes('AppTopbar'))failures.push('Az AppLayout nem a külön AppTopbar komponenst használja.');if(!appLayout.includes('useSessionIdleGuard'))failures.push('Az AppLayout nem a külön session idle hookot használja.');if(!appLayout.includes('deriveRoleFlags')||!appLayout.includes('resolveCurrentPageHu'))failures.push('Az AppLayout szerepkör- vagy oldalcím-logikája nincs külön modellbe szervezve.');if(appLayout.includes('modern-topbar')||appLayout.includes('clearAuthenticatedSession')||appLayout.includes('const pageNames'))failures.push('Az AppLayoutba visszakerült topbar/session/page-meta implementáció.');if(!appTopbar.includes('LanguageSwitcher')||!appTopbar.includes('topbar-breadcrumb')||!appTopbar.includes('topbar-location')||!appTopbar.includes('topbar-logout'))failures.push('Az AppTopbar szerkezete hiányos.');if(!sessionIdleGuard.includes('IDLE_TIMEOUT_MS')||!sessionIdleGuard.includes('visibilitychange')||!sessionIdleGuard.includes('clearAuthenticatedSession'))failures.push('A session idle guardból hiányzik a korábbi biztonsági viselkedés.');if(!appLayoutModel.includes('PAGE_NAMES')||!appLayoutModel.includes('deriveRoleFlags')||!appLayoutModel.includes('resolveCurrentPageHu'))failures.push('Az AppLayout modellből hiányzik route- vagy role-meta logika.');if(!appLayoutCss.includes('--topbar-font-sm: .8125rem')||!appLayoutCss.includes('font-size:var(--topbar-font-sm)'))failures.push('A felső sáv tipográfiája nincs skálázható rem/token alapra állítva.');if(/\.topbar-(?:breadcrumb|global-search|location|profile|logout)[^{]*\{[^}]*font-size\s*:\s*\d+px/s.test(appLayoutCss))failures.push('A felső sávban fix px-es font-size maradt.');const workflowDir='.github/workflows';if(fs.existsSync(workflowDir)){for(const name of fs.readdirSync(workflowDir).filter(file=>/\.ya?ml$/i.test(file))){const workflow=fs.readFileSync(path.join(workflowDir,name),'utf8');const writesContents=/contents\s*:\s*write/i.test(workflow);const pushesMain=/git\s+push[^\n]*(?:HEAD:main|origin\s+main|refs\/heads\/main)/i.test(workflow);if(writesContents&&pushesMain)failures.push(`Közvetlen main-forrásmódosító workflow tiltott: ${name}`)}}const forbiddenDashboardFallback=/function\s+FallbackRedirect\s*\(\s*\)\s*\{[\s\S]{0,320}<Navigate\s+to=\{getToken\(\)\?HOME_PATH/.test(routeAccess);if(forbiddenDashboardFallback)failures.push('Az ismeretlen route még mindig csendben az irányítópultra irányít.');if(!/MENU_CACHE_KEY='kleo\.menu\.cache\.v\d+'/.test(sidebar))failures.push('A menü-cache kulcs nincs verziózva.');if(failures.length){console.error('MENÜ / ROUTING ARCHITEKTÚRA AUDIT HIBA');failures.forEach(x=>console.error(' - '+x));process.exit(1)}console.log(`Routing audit OK: ${routeMatches.length} egyedi route, ${routeFiles.length} domain modul, ${critical.length} kritikus router, ${scopedMenuRoutes.length} statikus szerepkör-menü útvonal, moduláris AppLayout és 100%-os dinamikus font-scale kontroll ellenőrizve.`);