import{useEffect}from"react";import api from"../api/api";import{applyVirCustomization,readStoredVirCustomization,VIR_CUSTOMIZATION_EVENT}from"../utils/virCustomization";
export{VIR_CUSTOMIZATION_EVENT};
export{applyVirCustomization};
export default function VirCustomizationRuntime(){useEffect(()=>{applyVirCustomization(readStoredVirCustomization(),{broadcast:false});let active=true;const load=async()=>{const token=localStorage.getItem("kleo_token")||localStorage.getItem("token");if(!token)return;try{const r=await api.get("/transactions/knowledge-base/vir-customizer");if(active)applyVirCustomization(r.data?.config)}catch{}};void load();const onCustom=(e:Event)=>applyVirCustomization((e as CustomEvent).detail,{broadcast:false});const onFocus=()=>void load();window.addEventListener(VIR_CUSTOMIZATION_EVENT,onCustom);window.addEventListener("focus",onFocus);const id=window.setInterval(load,60_000);return()=>{active=false;window.removeEventListener(VIR_CUSTOMIZATION_EVENT,onCustom);window.removeEventListener("focus",onFocus);window.clearInterval(id)}},[]);return null}
