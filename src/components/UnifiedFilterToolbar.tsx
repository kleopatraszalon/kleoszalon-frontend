import React from"react";import{RotateCcw,Search}from"lucide-react";import{useLanguage}from"../i18n/LanguageProvider";
export type FilterOption={value:string;label:string};export type FilterSelect={key:string;value:string;options:FilterOption[];onChange:(value:string)=>void};
type Props={query:string;onQueryChange:(value:string)=>void;placeholder?:string;selects?:FilterSelect[];onApply?:()=>void;onReset?:()=>void;count?:number;actions?:React.ReactNode};
export default function UnifiedFilterToolbar({query,onQueryChange,placeholder,selects=[],onApply,onReset,count,actions}:Props){const{t}=useLanguage();return <div className="unified-filter-toolbar" style={{display:"flex",alignItems:"center",gap:8,flexWrap:"wrap",padding:10,border:"1px solid #e6ddd7",borderRadius:12,background:"#fff"}}><label style={{display:"flex",alignItems:"center",gap:7,flex:1,minWidth:260}}><Search size={16}/><input style={{width:"100%",height:38,border:"1px solid #ded3cc",borderRadius:9,padding:"0 10px"}} value={query} onChange={e=>onQueryChange(e.target.value)} onKeyDown={e=>{if(e.key==="Enter")onApply?.()}} placeholder={placeholder||t("common.search")}/></label>{selects.map(s=><select key={s.key} value={s.value} onChange={e=>s.onChange(e.target.value)} style={{height:38,border:"1px solid #ded3cc",borderRadius:9,padding:"0 9px",background:"#fff"}}>{s.options.map(o=><option key={o.value} value={o.value}>{o.label}</option>)}</select>)}{onReset&&<button type="button" onClick={onReset} style={{height:38,display:"inline-flex",alignItems:"center",gap:6}}><RotateCcw size={15}/>{t("common.reset")}</button>}{onApply&&<button type="button" onClick={onApply} style={{height:38}}>{t("common.filter")}</button>}{actions}{typeof count==="number"&&<span style={{marginLeft:"auto",fontSize:12,color:"#78695f"}}><b>{count}</b> {t("common.results")}</span>}</div>}
