import fs from"fs";import path from"path";
const source=fs.readFileSync(path.join(__dirname,"KleopatraMobileApp.tsx"),"utf8");
test("app has login, registration and guest entry",()=>{expect(source).toContain("Belépés");expect(source).toContain("Regisztráció");expect(source).toContain("Folytatás vendégként");expect(source).toContain("Minden ami szépség, csak Neked!")});
test("registered customer account exposes wallet and passes",()=>{expect(source).toContain("/customer-portal/dashboard");expect(source).toContain("Vendégegyenleg");expect(source).toContain("Aktív bérletek")});
test("admin login and app version notifications are supported",()=>{expect(source).toContain('"customer","admin"');expect(source).toContain("Admin felület");expect(source).toContain("app-version.json");expect(source).toContain("showNotification");expect(source).toContain('APP_VERSION="1.4.0"')});
test("beauty assistant and inactivity heartbeat are active",()=>{expect(source).toContain("Kleopátra szépségsegéd");expect(source).toContain("daily-actions/assistant");expect(source).toContain("daily-actions/heartbeat");expect(source).toContain("BIZTONSÁGOS ALAP AJÁNLÓ")});
test("install is always visible with separate Android and iPhone actions",()=>{expect(source).toContain("Androidra");expect(source).toContain("iPhone-ra");expect(source).toContain("Android · Chrome vagy Edge");expect(source).toContain("iPhone · Safari");expect(source).toContain("Főképernyőhöz adás")});
