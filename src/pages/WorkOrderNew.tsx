import React, { useEffect, useMemo, useState } from "react";
import { useNavigate } from "react-router-dom";
import { apiFetch } from "../utils/api";
import api from "../api";
import WorkOrderLifecyclePanel, { WorkOrderLifecycleStatus } from "./workorders/WorkOrderLifecyclePanel";
import WorkOrderMaterialsPanel, { WorkOrderMaterial } from "./workorders/WorkOrderMaterialsPanel";
import WorkOrderLoyaltyPanel, { LoyaltySelection } from "./workorders/WorkOrderLoyaltyPanel";

type Employee = { id: string | number; full_name?: string; display_name?: string };
type Service = { id: string | number; name: string; duration_minutes?: number | null; default_duration?: number | null; price?: number | null; price_gross?: number | null };
type Product = { id: string | number; name: string; price?: number | null; price_gross?: number | null; unit?: string | null; stock_quantity?: number | null; available_stock?: number | null; quantity?: number | null };
type WorkOrderPayload = { title: string; notes: string; status: WorkOrderLifecycleStatus; started_at?: string | null; completed_at?: string | null; employee_id?: string | number; client_name?: string; client_phone?: string; client_email?: string; fully_paid?: boolean; note_for_another_visitor?: boolean; services?: { service_id: string | number; quantity: number }[]; products?: { product_id: string | number; quantity: number }[] };
const emptyLoyalty:LoyaltySelection={walletAmount:0,pointsToSpend:0,couponCode:"",voucherCode:"",voucherAmount:0,passUsages:[]};

const WorkOrderNew: React.FC = () => {
  const navigate = useNavigate();
  const [form, setForm] = useState({ title: "", notes: "", clientName: "", clientPhone: "", clientEmail: "", noteForAnotherVisitor: false, fullyPaid: false, status: "arrived" as WorkOrderLifecycleStatus });
  const [startedAt, setStartedAt] = useState<string | null>(null); const [completedAt, setCompletedAt] = useState<string | null>(null);
  const [employees, setEmployees] = useState<Employee[]>([]); const [selectedEmployeeId, setSelectedEmployeeId] = useState("");
  const [services, setServices] = useState<Service[]>([]); const [products, setProducts] = useState<Product[]>([]);
  const [serviceSearch, setServiceSearch] = useState(""); const [productSearch, setProductSearch] = useState("");
  const [selectedServiceIds, setSelectedServiceIds] = useState<string[]>([]); const [materials, setMaterials] = useState<WorkOrderMaterial[]>([]);
  const [activeTab, setActiveTab] = useState<"services" | "products">("services"); const [saving, setSaving] = useState(false); const [error, setError] = useState<string | null>(null);
  const [loyalty,setLoyalty]=useState<LoyaltySelection>(emptyLoyalty);
  useEffect(() => { Promise.all([apiFetch<Employee[]>("/api/employees"), apiFetch<Product[]>("/api/products")]).then(([employeeData, productData]) => { if (Array.isArray(employeeData)) setEmployees(employeeData); if (Array.isArray(productData)) setProducts(productData); }).catch((reason) => console.error("Munkalap törzsadat betöltési hiba", reason)); }, []);
  const handleEmployeeChange = async (event: React.ChangeEvent<HTMLSelectElement>) => { const employeeId = event.target.value; setSelectedEmployeeId(employeeId); setSelectedServiceIds([]); if (!employeeId) return setServices([]); try { const data = await apiFetch<Service[]>(`/api/services?employee_id=${encodeURIComponent(employeeId)}`); setServices(Array.isArray(data) ? data : []); } catch (reason) { console.error("Szolgáltatások betöltési hiba", reason); setServices([]); } };
  const selectedServices = useMemo(() => services.filter((item) => selectedServiceIds.includes(String(item.id))), [services, selectedServiceIds]);
  const selectedProductIds = useMemo(() => materials.map((item) => String(item.productId)), [materials]);
  const filteredServices = useMemo(() => services.filter((item) => item.name.toLowerCase().includes(serviceSearch.trim().toLowerCase())), [services, serviceSearch]);
  const filteredProducts = useMemo(() => products.filter((item) => item.name.toLowerCase().includes(productSearch.trim().toLowerCase())), [products, productSearch]);
  const totalDuration = useMemo(() => selectedServices.reduce((sum, item) => sum + Number(item.duration_minutes ?? item.default_duration ?? 0), 0), [selectedServices]);
  const serviceTotal = useMemo(() => selectedServices.reduce((sum, item) => sum + Number(item.price_gross ?? item.price ?? 0), 0), [selectedServices]);
  const materialTotal = useMemo(() => materials.reduce((sum, item) => sum + Number(item.unitPrice ?? 0) * item.quantity, 0), [materials]);
  const totalPrice = serviceTotal + materialTotal;
  const handleLifecycleChange = (status: WorkOrderLifecycleStatus) => { const now = new Date().toISOString(); setForm((current) => ({ ...current, status })); if (status === "in_progress" && !startedAt) setStartedAt(now); if (status === "completed") { if (!startedAt) setStartedAt(now); setCompletedAt(now); } else if (completedAt) setCompletedAt(null); setError(null); };
  const toggleService = (service: Service) => { const id = String(service.id); setSelectedServiceIds((current) => current.includes(id) ? current.filter((value) => value !== id) : [...current, id]); if (!form.title.trim()) setForm((current) => ({ ...current, title: service.name })); };
  const toggleProduct = (product: Product) => { const exists = materials.some((item) => String(item.productId) === String(product.id)); if (exists) return setMaterials((current) => current.filter((item) => String(item.productId) !== String(product.id))); setMaterials((current) => [...current,{ productId: product.id, name: product.name, quantity: 1, unit: product.unit || "db", unitPrice: Number(product.price_gross ?? product.price ?? 0), availableStock: product.available_stock ?? product.stock_quantity ?? product.quantity ?? null }]); };
  const submit = async (event: React.FormEvent) => { event.preventDefault(); if (!form.title.trim() && !selectedServices.length && !materials.length) return setError("Adj meg egy címet vagy válassz legalább egy szolgáltatást / terméket."); const invalidMaterial = materials.find((item) => item.quantity <= 0 || (item.availableStock != null && item.quantity > Number(item.availableStock))); if (invalidMaterial) return setError(`Ellenőrizd a(z) „${invalidMaterial.name}” anyagmennyiségét és készletét.`); const payload: WorkOrderPayload = { title: form.title.trim() || selectedServices[0]?.name || materials[0]?.name || "Munkalap", notes: form.notes, status: form.status, started_at: startedAt, completed_at: completedAt, employee_id: selectedEmployeeId || undefined, client_name: form.clientName || undefined, client_phone: form.clientPhone || undefined, client_email: form.clientEmail || undefined, fully_paid: form.fullyPaid, note_for_another_visitor: form.noteForAnotherVisitor, services: selectedServiceIds.map((service_id) => ({ service_id, quantity: 1 })), products: materials.map((item) => ({ product_id: item.productId, quantity: item.quantity })) }; try { setSaving(true); setError(null); const data = await apiFetch<{ id?: string | number }>("/api/workorders", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }); const workOrderId=data?.id?String(data.id):""; if(workOrderId&&(loyalty.accountId||loyalty.couponCode||loyalty.voucherCode||loyalty.passUsages.length)){await api.post('/api/transactions/loyalty-operations/checkout/apply',{work_order_id:workOrderId,account_id:loyalty.accountId||null,customer_id:loyalty.customerId||null,gross_amount:totalPrice,wallet_amount:loyalty.walletAmount,points_to_spend:loyalty.pointsToSpend,coupon_code:loyalty.couponCode||null,voucher_code:loyalty.voucherCode||null,voucher_amount:loyalty.voucherAmount,pass_usages:loyalty.passUsages});} navigate(workOrderId ? `/workorders/${workOrderId}` : "/workorders"); } catch (reason: any) { setError(reason?.response?.data?.message||reason?.message || "Hiba a munkalap mentése / hűségkedvezmény alkalmazása során"); } finally { setSaving(false); } };
  return <div className="home-container app-shell app-shell--collapsed"><main className="calendar-container"><div style={{ padding: 20, background: "#f5f6fa", minHeight: "100vh" }}><form onSubmit={submit}><WorkOrderLifecyclePanel status={form.status} startedAt={startedAt} completedAt={completedAt} onChange={handleLifecycleChange} disabled={saving}/><div style={{ display: "grid", gridTemplateColumns: "300px 1fr 380px", gap: 18, alignItems: "start", marginTop: 18 }}><aside style={panelStyle}><div style={panelTitle}>Munkalap</div><div style={smallMuted}>Lépésről lépésre rögzítés</div><label style={fieldStyle}>Munkatárs<select value={selectedEmployeeId} onChange={handleEmployeeChange} style={inputStyle}><option value="">Válassz munkatársat</option>{employees.map((item) => <option key={String(item.id)} value={String(item.id)}>{item.display_name || item.full_name || `#${item.id}`}</option>)}</select></label><label style={fieldStyle}>Megjegyzés<textarea value={form.notes} onChange={(event) => setForm((current) => ({ ...current, notes: event.target.value }))} rows={6} style={{ ...inputStyle, height: 120, paddingTop: 10 }} placeholder="Belső megjegyzés"/></label><label style={checkLabel}><input type="checkbox" checked={form.noteForAnotherVisitor} onChange={(event) => setForm((current) => ({ ...current, noteForAnotherVisitor: event.target.checked }))}/> Bejegyzés egy másik látogató számára</label><label style={checkLabel}><input type="checkbox" checked={form.fullyPaid} onChange={(event) => setForm((current) => ({ ...current, fullyPaid: event.target.checked }))}/> Teljesen kifizetve</label></aside><section style={panelStyle}><div style={{ display: "flex", justifyContent: "space-between", gap: 12 }}><div><div style={panelTitle}>Szolgáltatások és termékek</div><div style={smallMuted}>Az összesítő automatikusan számol</div></div><div><button type="button" style={activeTab === "services" ? tabActive : tabStyle} onClick={() => setActiveTab("services")}>Szolgáltatások</button><button type="button" style={activeTab === "products" ? tabActive : tabStyle} onClick={() => setActiveTab("products")}>Termékek</button></div></div>{activeTab === "services" ? <><input value={serviceSearch} onChange={(event) => setServiceSearch(event.target.value)} placeholder="Keresés szolgáltatások szerint" style={{ ...inputStyle, marginTop: 14 }}/><div style={cardsGrid}>{filteredServices.map((item) => <button key={String(item.id)} type="button" onClick={() => toggleService(item)} style={selectedServiceIds.includes(String(item.id)) ? cardActive : card}><b>{item.name}</b><small>{Number(item.price_gross ?? item.price ?? 0).toLocaleString("hu-HU")} Ft · {Number(item.duration_minutes ?? item.default_duration ?? 0)} perc</small></button>)}</div></> : <><input value={productSearch} onChange={(event) => setProductSearch(event.target.value)} placeholder="Keresés termékek szerint" style={{ ...inputStyle, marginTop: 14 }}/><div style={cardsGrid}>{filteredProducts.map((item) => <button key={String(item.id)} type="button" onClick={() => toggleProduct(item)} style={selectedProductIds.includes(String(item.id)) ? cardActive : card}><b>{item.name}</b><small>{Number(item.price_gross ?? item.price ?? 0).toLocaleString("hu-HU")} Ft</small></button>)}</div></>}</section><aside style={panelStyle}><div style={panelTitle}>Vendég és összesítő</div><label style={fieldStyle}>Név<input value={form.clientName} onChange={(event) => setForm((current) => ({ ...current, clientName: event.target.value }))} style={inputStyle}/></label><label style={fieldStyle}>Telefonszám<input value={form.clientPhone} onChange={(event) => setForm((current) => ({ ...current, clientPhone: event.target.value }))} style={inputStyle}/></label><label style={fieldStyle}>E-mail<input value={form.clientEmail} onChange={(event) => setForm((current) => ({ ...current, clientEmail: event.target.value }))} style={inputStyle}/></label><label style={fieldStyle}>Munkalap címe<input value={form.title} onChange={(event) => setForm((current) => ({ ...current, title: event.target.value }))} style={inputStyle}/></label><div style={summaryStyle}><div style={sumRow}><span>Szolgáltatások</span><b>{selectedServices.length}</b></div><div style={sumRow}><span>Anyagtételek</span><b>{materials.length}</b></div><div style={sumRow}><span>Időtartam</span><b>{totalDuration} perc</b></div><div style={sumRow}><span>Anyagérték</span><b>{materialTotal.toLocaleString("hu-HU")} Ft</b></div><div style={sumStrong}><span>Fizetendő</span><b>{totalPrice.toLocaleString("hu-HU")} Ft</b></div></div>{error && <div style={{ color: "#b91c1c", marginTop: 14 }}>{error}</div>}<div style={{ display: "flex", gap: 10, marginTop: 18 }}><button type="button" onClick={() => navigate("/workorders")} style={secondaryBtn}>Mégse</button><button type="submit" disabled={saving} style={primaryBtn}>{saving ? "Mentés..." : "Munkalap mentése"}</button></div></aside></div><WorkOrderLoyaltyPanel grossTotal={totalPrice} disabled={saving} onChange={setLoyalty}/><div style={{ marginTop: 18 }}><WorkOrderMaterialsPanel materials={materials} onChange={setMaterials} disabled={saving}/></div></form></div></main></div>;
};
const panelStyle: React.CSSProperties = { background: "#fff", borderRadius: 18, padding: 18, boxShadow: "0 8px 24px rgba(0,0,0,.08)" };const panelTitle: React.CSSProperties = { fontSize: 22, fontWeight: 900 }; const smallMuted: React.CSSProperties = { fontSize: 13, color: "#667085" };const fieldStyle: React.CSSProperties = { display: "grid", gap: 6, marginTop: 16, fontSize: 13, fontWeight: 700 }; const inputStyle: React.CSSProperties = { width: "100%", height: 40, borderRadius: 10, border: "1px solid #d0d5dd", padding: "0 12px", boxSizing: "border-box", background: "#fff" };const checkLabel: React.CSSProperties = { display: "flex", gap: 8, alignItems: "center", marginTop: 14 }; const tabStyle: React.CSSProperties = { border: "1px solid #d0d5dd", background: "#fff", borderRadius: 10, height: 36, padding: "0 12px", cursor: "pointer", marginLeft: 8 }; const tabActive: React.CSSProperties = { ...tabStyle, background: "#f4f3ff", borderColor: "#c7d2fe", color: "#4338ca", fontWeight: 700 };const cardsGrid: React.CSSProperties = { display: "grid", gridTemplateColumns: "repeat(auto-fill,minmax(180px,1fr))", gap: 12, marginTop: 14 }; const card: React.CSSProperties = { display: "grid", gap: 8, textAlign: "left", border: "1px solid #e5e7eb", background: "#fff", borderRadius: 14, padding: 14, cursor: "pointer" }; const cardActive: React.CSSProperties = { ...card, borderColor: "#6366f1", background: "#eef2ff" };const summaryStyle: React.CSSProperties = { marginTop: 18, padding: 14, borderRadius: 12, background: "#f8fafc", border: "1px solid #e5e7eb" }; const sumRow: React.CSSProperties = { display: "flex", justifyContent: "space-between", marginTop: 8, color: "#475467" }; const sumStrong: React.CSSProperties = { display: "flex", justifyContent: "space-between", marginTop: 12, fontSize: 18 };const primaryBtn: React.CSSProperties = { flex: 1, height: 42, border: 0, borderRadius: 10, background: "#4f46e5", color: "#fff", fontWeight: 700, cursor: "pointer" }; const secondaryBtn: React.CSSProperties = { flex: 1, height: 42, border: "1px solid #d0d5dd", borderRadius: 10, background: "#fff", fontWeight: 700, cursor: "pointer" };export default WorkOrderNew;
