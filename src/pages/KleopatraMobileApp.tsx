import React,{useEffect,useState}from"react";import{Bell,CalendarDays,Crown,Download,Gift,Home,MapPin,Scissors,Sparkles,WalletCards}from"lucide-react";import api from"../api/api";import"./KleopatraMobileApp.css";function keyToUint(v:string){const p='='.repeat((4-v.length%4)%4),b=atob((v+p).replace(/-/g,'+').replace(/_/g,'/'));return Uint8Array.from([...b].map(x=>x.charCodeAt(0)))}export default function KleopatraMobileApp(){const[data,setData]=useState<any>({actions:[],vapid_public_key:null}),[install,setInstall]=useState<any>(null),[enabled,setEnabled]=useState(Notification?.permission==='granted');useEffect(()=>{api.get('/public/marketing/daily-actions').then(r=>setData(r.data)).catch(()=>{});const h=(e:any)=>{e.preventDefault();setInstall(e)};window.addEventListener('beforeinstallprompt',h);return()=>window.removeEventListener('beforeinstallprompt',h)},[]);async function notify(){try{const perm=await Notification.requestPermission();if(perm!=='granted')return;const reg=await navigator.serviceWorker.ready;if(data.vapid_public_key){const sub=await reg.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:keyToUint(data.vapid_public_key)});await api.post('/public/marketing/daily-actions/subscribe',{subscription:sub})}setEnabled(true);new Notification('Kleopátra App',{body:'Értesítések bekapcsolva. A napi akciókról azonnal szólunk.',icon:'/logo192.png'})}catch{}}return <main className="kapp"><header><div className="kapp-logo"><Crown/><span><b>KLEOPÁTRA</b><small>BEAUTY CLUB</small></span></div><div>{install&&<button onClick={()=>install.prompt()}><Download/>Telepítés</button>}<button className={enabled?'enabled':''} onClick={notify}><Bell/>{enabled?'Értesítés aktív':'Értesítés'}</button></div></header><section className="kapp-hero"><div><small>AZ ÖN SZÉPSÉGE. AZ ÖN PILLANATA.</small><h1>Ragyogjon minden nap.</h1><p>Foglalás, exkluzív ajánlatok és hűségelőnyök egy elegáns, személyes alkalmazásban.</p><a href="/foglalas"><CalendarDays/>Időpontot foglalok</a></div><div className="orb"><Sparkles/></div></section><section className="kapp-quick"><a href="/foglalas"><Scissors/><span><b>Gyors foglalás</b><small>3 egyszerű lépés</small></span></a><button><WalletCards/><span><b>Beauty Pass</b><small>Bérlet és pontok</small></span></button><button><Gift/><span><b>Ajándékok</b><small>Kuponok és utalványok</small></span></button><button><MapPin/><span><b>Szalonok</b><small>Legközelebbi hely</small></span></button></section><section className="kapp-deals"><header><div><small>CSAK MA</small><h2>Napi akciók</h2></div><span>{data.actions.length} ajánlat</span></header><div>{data.actions.length?data.actions.map((x:any)=><article key={x.id}>{x.image_url&&<img src={x.image_url} alt=""/>}<div><small>KLEOPÁTRA EXKLUZÍV</small><h3>{x.headline}</h3><strong>{x.discount_text}</strong><div dangerouslySetInnerHTML={{__html:x.description_html}}/><a href={x.cta_url||'/foglalas'}>{x.cta_label||'Foglalok'}</a></div></article>):<article className="empty-deal"><Sparkles/><div><h3>A következő meglepetés készül</h3><p>Kapcsolja be az értesítéseket, hogy elsőként értesüljön.</p></div></article>}</div></section><nav><button className="active"><Home/><span>Kezdőlap</span></button><a href="/foglalas"><CalendarDays/><span>Foglalás</span></a><button><Crown/><span>Klub</span></button><button><Bell/><span>Értesítések</span></button></nav></main>}
