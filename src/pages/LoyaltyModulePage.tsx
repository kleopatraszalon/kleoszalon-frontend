import React,{useEffect,useMemo,useState}from'react';import{useLocation,useNavigate}from'react-router-dom';import{Gift,WalletCards,TicketPercent,BadgePercent,Repeat2,RefreshCw}from'lucide-react';import api from'../api';import'./LoyaltyModulePage.css';
const money=(v:any)=>`${Number(v||0).toLocaleString('hu-HU',{maximumFractionDigits:0})} Ft`;const num=(v:any)=>Number(v||0).toLocaleString('hu-HU');
const tabs=[['/loyalty','Áttekintés'],['/loyalty/accounts','Vendég egyenlegek'],['/loyalty/points','Hűségpontok'],['/loyalty/passes','Bérletek'],['/loyalty/coupons','Kuponok'],['/loyalty/vouchers','Ajándékutalványok'],['/loyalty/transactions','Tranzakciók']];
export default function LoyaltyModulePage(){const nav=useNavigate(),loc=useLocation();const section=loc.pathname.split('/')[2]||'overview';const[summary,setSummary]=useState<any>({});const[rows,setRows]=useState<any[]>([]);const[loading,setLoading]=useState(false);const[error,setError]=useState('');const[form,setForm]=useState<any>({});const endpoint=useMemo(()=>section==='accounts'?'/api/transactions/loyalty/accounts':section==='passes'?'/api/transactions/loyalty/passes':section==='coupons'?'/api/transactions/loyalty/coupon-campaigns':section==='vouchers'?'/api/transactions/loyalty/vouchers':section==='transactions'?'/api/transactions/loyalty-analytics/transactions':section==='points'?'/api/transactions/loyalty-analytics/points-rules':'',[section]);async function load(){setLoading(true);setError('');try{const s=await api.get('/api/transactions/loyalty-analytics/summary');setSummary(s.data||{});if(endpoint){const r=await api.get(endpoint);setRows(Array.isArray(r.data)?r.data:[])}}catch(e:any){setError(e?.response?.data?.message||'A hűségmodul adatai nem tölthetők be.')}finally{setLoading(false)}}useEffect(()=>{void load()},[endpoint]);async function create(){try{if(section==='accounts')await api.post('/api/transactions/loyalty/accounts',form);if(section==='passes')await api.post('/api/transactions/loyalty/pass-types',{name:form.name,valid_days:Number(form.valid_days||0)||null});if(section==='coupons')await api.post('/api/transactions/loyalty/coupon-campaigns',{name:form.name,discount_type:form.discount_type||'percent',discount_value:Number(form.discount_value||0),generate_count:Number(form.generate_count||0),usage_mode:form.usage_mode||'single'});if(section==='vouchers')await api.post('/api/transactions/loyalty/vouchers',{value:Number(form.value||0),owner_customer_id:form.owner_customer_id||null});if(section==='points')await api.post('/api/transactions/loyalty-analytics/points-rules',{name:form.name,spend_amount:Number(form.spend_amount||100),points_earned:Number(form.points_earned||1),point_value:Number(form.point_value||1)});setForm({});await load()}catch(e:any){setError(e?.response?.data?.message||'A mentés nem sikerült.')}}
return <div className="loyalty-page"><section className="loyalty-hero"><div><span>VENDÉGMEGTARTÁS · WALLET</span><h1><Gift size={28}/> Hűség, bérletek és ajándékkártyák</h1><p>Egyenleg, hűségpont, bérlet, kupon és ajándékutalvány egy vendégközpontú rendszerben.</p></div><button onClick={()=>void load()}><RefreshCw size={16}/> Frissítés</button></section><div className="loyalty-tabs">{tabs.map(([p,l])=><button key={p} className={loc.pathname===p?'active':''} onClick={()=>nav(p)}>{l}</button>)}</div>{error&&<div className="loyalty-error">{error}</div>}<section className="loyalty-kpis"><K icon={<WalletCards/>} label="Vendégegyenleg" value={money(summary.wallet_balance)}/><K icon={<BadgePercent/>} label="Hűségpont" value={num(summary.points_balance)}/><K icon={<Repeat2/>} label="Aktív bérlet" value={num(summary.active_passes)}/><K icon={<TicketPercent/>} label="Aktív kupon" value={num(summary.active_coupons)}/><K icon={<Gift/>} label="Aktív utalvány" value={num(summary.active_vouchers)}/><K icon={<Gift/>} label="Utalvány kötelezettség" value={money(summary.voucher_liability)}/></section>{section==='overview'?<Overview s={summary}/>:<section className="loyalty-grid"><article className="loyalty-panel"><h2>{title(section)} létrehozása</h2><Editor section={section} form={form} setForm={setForm}/><div className="loyalty-actions" style={{marginTop:12}}><button onClick={create}>Mentés</button></div></article><article className="loyalty-panel"><h2>{title(section)}</h2>{loading?<div className="loyalty-empty">Betöltés…</div>:<Table section={section} rows={rows}/>}</article></section>}</div>}
function K({icon,label,value}:{icon:any,label:string,value:string}){return <article className="loyalty-kpi">{icon}<small>{label}</small><strong>{value}</strong></article>}
function Overview({s}:{s:any}){return <section className="loyalty-grid"><article className="loyalty-panel"><h2>Aktuális hűségállomány</h2><table className="loyalty-table"><tbody><tr><td>Aktív vendég-wallet</td><td><b>{num(s.active_accounts)}</b></td></tr><tr><td>Felhasználható bérletegység</td><td><b>{num(s.pass_units_remaining)}</b></td></tr><tr><td>Kuponfelhasználások</td><td><b>{num(s.coupon_uses)}</b></td></tr><tr><td>30 napon belül lejáró utalvány</td><td><b>{num(s.vouchers_expiring_30d)}</b></td></tr></tbody></table></article><article className="loyalty-panel"><h2>Időszaki aktivitás</h2><table className="loyalty-table"><tbody><tr><td>Egyenlegfeltöltés</td><td><b>{money(s.period_topups)}</b></td></tr><tr><td>Beváltások</td><td><b>{money(s.period_redemptions)}</b></td></tr><tr><td>Szerzett pontok</td><td><b>{num(s.period_points_earned)}</b></td></tr><tr><td>Felhasznált pontok</td><td><b>{num(s.period_points_spent)}</b></td></tr></tbody></table></article></section>}
function Editor({section,form,setForm}:{section:string,form:any,setForm:any}){const f=(k:string,p?:any)=>(e:any)=>setForm({...form,[k]:p? p(e.target.value):e.target.value});if(section==='accounts')return <div className="loyalty-form"><input placeholder="Vendég azonosító" value={form.customer_id||''} onChange={f('customer_id')}/><input placeholder="Kártya / RFID azonosító" value={form.card_identifier||''} onChange={f('card_identifier')}/></div>;if(section==='points')return <div className="loyalty-form"><input placeholder="Szabály neve" value={form.name||''} onChange={f('name')}/><input type="number" placeholder="Költés Ft" value={form.spend_amount||''} onChange={f('spend_amount')}/><input type="number" placeholder="Kapott pont" value={form.points_earned||''} onChange={f('points_earned')}/><input type="number" placeholder="1 pont értéke Ft" value={form.point_value||''} onChange={f('point_value')}/></div>;if(section==='passes')return <div className="loyalty-form"><input placeholder="Bérlettípus neve" value={form.name||''} onChange={f('name')}/><input type="number" placeholder="Érvényesség nap" value={form.valid_days||''} onChange={f('valid_days')}/></div>;if(section==='coupons')return <div className="loyalty-form"><input placeholder="Kampány neve" value={form.name||''} onChange={f('name')}/><select value={form.discount_type||'percent'} onChange={f('discount_type')}><option value="percent">Százalék</option><option value="amount">Fix összeg</option></select><input type="number" placeholder="Kedvezmény" value={form.discount_value||''} onChange={f('discount_value')}/><input type="number" placeholder="Generált kódok száma" value={form.generate_count||''} onChange={f('generate_count')}/></div>;if(section==='vouchers')return <div className="loyalty-form"><input type="number" placeholder="Utalvány értéke" value={form.value||''} onChange={f('value')}/><input placeholder="Tulajdonos vendég ID" value={form.owner_customer_id||''} onChange={f('owner_customer_id')}/></div>;return <div className="loyalty-empty">A tranzakciók automatikusan keletkeznek.</div>}
function Table({section,rows}:{section:string,rows:any[]}){if(!rows.length)return <div className="loyalty-empty">Még nincs adat.</div>;const cols=section==='accounts'?[['customer_id','Vendég'],['balance','Egyenleg'],['points','Pont'],['card_identifier','Kártya']]:section==='points'?[['name','Szabály'],['spend_amount','Költés'],['points_earned','Pont'],['point_value','Pontérték']]:section==='passes'?[['pass_type_name','Bérlet'],['customer_id','Vendég'],['valid_from','Kezdet'],['valid_until','Lejárat'],['status','Státusz']]:section==='coupons'?[['name','Kampány'],['discount_type','Típus'],['discount_value','Érték'],['code_count','Kódok']]:section==='vouchers'?[['code','Kód'],['owner_customer_id','Tulajdonos'],['original_value','Eredeti'],['remaining_value','Maradvány'],['status','Státusz']]:[['created_at','Idő'],['customer_id','Vendég'],['transaction_type','Típus'],['amount','Összeg'],['points','Pont']];return <table className="loyalty-table"><thead><tr>{cols.map(c=><th key={c[0]}>{c[1]}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={r.id||i}>{cols.map(c=><td key={c[0]}>{['balance','original_value','remaining_value','amount','spend_amount','point_value'].includes(c[0])?money(r[c[0]]):String(r[c[0]]??'—')}</td>)}</tr>)}</tbody></table>}
function title(s:string){return({accounts:'Vendég egyenlegek',points:'Hűségpontok',passes:'Bérletek',coupons:'Kuponok',vouchers:'Ajándékutalványok',transactions:'Tranzakciók'} as any)[s]||'Hűség'}