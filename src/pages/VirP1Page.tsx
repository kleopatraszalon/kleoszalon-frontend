import React,{useEffect,useMemo,useState}from"react";
import{useNavigate,useSearchParams}from"react-router-dom";
import{getLocations,type LocationRow}from"../api/locations";
import{getCompetencyMatrix,getCustomer360,getForecast,getInventoryIntelligence,getSimulator}from"../api/virP1";
import"./VirManagement.css";
type Tab="customer360"|"forecast"|"inventory"|"skills"|"simulator";
const money=(v:any)=>new Intl.NumberFormat("hu-HU",{style:"currency",currency:"HUF",maximumFractionDigits:0}).format(Number(v||0));
const num=(v:any)=>new Intl.NumberFormat("hu-HU").format(Number(v||0));
export default function VirP1Page(){const nav=useNavigate(),[sp,setSp]=useSearchParams();const tab=(sp.get("tab")||"customer360") as Tab;const[locations,setLocations]=useState<LocationRow[]>([]),[locationId,setLocationId]=useState(""),[data,setData]=useState<any>(null),[loading,setLoading]=useState(false),[error,setError]=useState("");const[sim,setSim]=useState({revenue_change:0,price_change:0,utilization_change:0,wage_change:0,material_change:0});useEffect(()=>{getLocations().then(setLocations).catch(()=>setLocations([]))},[]);useEffect(()=>{let live=true;setLoading(true);setError("");const params={locationId:locationId||undefined,...sim};const p=tab==="customer360"?getCustomer360(params):tab==="forecast"?getForecast(params):tab==="inventory"?getInventoryIntelligence(params):tab==="skills"?getCompetencyMatrix():getSimulator(params);p.then(x=>live&&setData(x)).catch((e:any)=>live&&setError(e?.response?.data?.error||e?.message||"Betöltési hiba")).finally(()=>live&&setLoading(false));return()=>{live=false}},[tab,locationId,sim]);const setTab=(x:Tab)=>setSp({tab:x});const skillSummary=useMemo(()=>{const rows=Array.isArray(data)?data:[];return{rows:rows.length,expert:rows.filter((x:any)=>Number(x.skill_level)>=4).length,expiring:rows.filter((x:any)=>x.qualification_status==='expiring').length,expired:rows.filter((x:any)=>x.qualification_status==='expired').length}},[data]);return <div className="vir-management-page"><div className="vir-management-header"><div><h1 className="vir-management-title">VIR P1 Intelligence</h1><div className="vir-management-subtitle">Customer 360, 7/30/90 napos forecast, intelligens készlet/beszerzés, kompetenciamátrix és üzleti szimulátor.</div></div><label className="vir-field"><span>Telephely</span><select value={locationId} onChange={e=>setLocationId(e.target.value)}><option value="">Összes telephely</option>{locations.map(l=><option key={l.id} value={l.id}>{l.name}</option>)}</select></label></div><div className="vir-management-actions" style={{marginBottom:18}}>{([['customer360','Customer 360'],['forecast','7/30/90 forecast'],['inventory','Intelligens készlet'],['skills','Kompetenciamátrix'],['simulator','Üzleti szimulátor']] as [Tab,string][]).map(([k,l])=><button key={k} className={`vir-button ${tab===k?'':'secondary'}`} onClick={()=>setTab(k)}>{l}</button>)}</div>{error&&<div className="vir-error">{error}</div>}{loading&&<div className="vir-spinner">VIR P1 elemzés frissítése…</div>}
{!loading&&tab==='customer360'&&data?.summary&&<><div className="vir-kpi-grid"><div className="vir-kpi"><div className="vir-kpi-label">Ügyfelek</div><div className="vir-kpi-value">{num(data.summary.clients)}</div></div><div className="vir-kpi is-good"><div className="vir-kpi-label">VIP</div><div className="vir-kpi-value">{num(data.summary.vip)}</div></div><div className="vir-kpi is-warning"><div className="vir-kpi-label">Visszacsábítandó</div><div className="vir-kpi-value">{num(data.summary.at_risk)}</div></div><div className="vir-kpi"><div className="vir-kpi-label">Ügyfél-élettartamérték</div><div className="vir-kpi-value">{money(data.summary.lifetime_value)}</div></div></div><section className="vir-panel"><div className="vir-panel-header"><div><div className="vir-panel-title">Customer 360 toplista</div><div className="vir-panel-muted">LTV, látogatás, recency és következő időpont alapján.</div></div><button className="vir-link-button" onClick={()=>nav('/modules/customers/intelligence')}>Next Best Action →</button></div><table className="vir-source-table"><thead><tr><th>Vendég</th><th>Tier</th><th>Látogatás</th><th>LTV</th><th>Utolsó látogatás</th><th>No-show</th></tr></thead><tbody>{data.items?.slice(0,40).map((x:any)=><tr key={x.client_id}><td>{x.client_name}</td><td>{x.value_tier}</td><td>{x.visits}</td><td>{money(x.lifetime_value)}</td><td>{x.recency_days==null?'—':`${x.recency_days} napja`}</td><td>{x.no_shows}</td></tr>)}</tbody></table></section></>}
{!loading&&tab==='forecast'&&data?.horizons&&<><div className="vir-kpi-grid">{data.horizons.map((x:any)=><div className="vir-kpi" key={x.days}><div className="vir-kpi-label">{x.days} napos forecast</div><div className="vir-kpi-value">{money(x.forecast_base)}</div><div className="vir-kpi-note">Sáv: {money(x.forecast_low)} – {money(x.forecast_high)} · foglalási lefedettség {x.coverage_percent}%</div></div>)}</div><section className="vir-panel"><div className="vir-panel-title">Forecast módszertan</div><div className="vir-panel-muted">90 napos realizált run-rate + a már lefoglalt pipeline mint alsó korlát. Napi run-rate: {money(data.daily_run_rate)}.</div></section></>}
{!loading&&tab==='inventory'&&data?.summary&&<><div className="vir-kpi-grid"><div className="vir-kpi is-critical"><div className="vir-kpi-label">Kritikus készlet</div><div className="vir-kpi-value">{data.summary.critical}</div></div><div className="vir-kpi is-warning"><div className="vir-kpi-label">Magas prioritás</div><div className="vir-kpi-value">{data.summary.high}</div></div><div className="vir-kpi"><div className="vir-kpi-label">Javasolt rendelési érték</div><div className="vir-kpi-value">{money(data.summary.suggested_order_value)}</div></div></div><section className="vir-panel"><div className="vir-panel-header"><div className="vir-panel-title">Készlet- és rendelési javaslatok</div><button className="vir-link-button" onClick={()=>nav('/warehouse?view=procurement&section=suggestions')}>Beszerzési javaslatok →</button></div><table className="vir-source-table"><thead><tr><th>Termék</th><th>Telephely</th><th>Készlet</th><th>30 napi fogyás</th><th>Napfedezet</th><th>Javasolt rendelés</th><th>Prioritás</th></tr></thead><tbody>{data.items?.slice(0,80).map((x:any)=><tr key={`${x.location_id}-${x.product_id}`}><td>{x.product_name}</td><td>{x.location_name}</td><td>{x.stock_qty}</td><td>{x.used_30d}</td><td>{x.days_cover??'—'}</td><td>{x.suggested_order_qty} / {money(x.suggested_order_value)}</td><td>{x.priority}</td></tr>)}</tbody></table></section></>}
{!loading&&tab==='skills'&&<><div className="vir-kpi-grid"><div className="vir-kpi"><div className="vir-kpi-label">Kompetenciasor</div><div className="vir-kpi-value">{skillSummary.rows}</div></div><div className="vir-kpi is-good"><div className="vir-kpi-label">Senior / Expert</div><div className="vir-kpi-value">{skillSummary.expert}</div></div><div className="vir-kpi is-warning"><div className="vir-kpi-label">30 napon belül lejár</div><div className="vir-kpi-value">{skillSummary.expiring}</div></div><div className="vir-kpi is-critical"><div className="vir-kpi-label">Lejárt képesítés</div><div className="vir-kpi-value">{skillSummary.expired}</div></div></div><section className="vir-panel"><div className="vir-panel-header"><div><div className="vir-panel-title">Kompetenciamátrix</div><div className="vir-panel-muted">A kanonikus HR skill-mátrix szolgáltatási jogosultságot, 1–5 skill-szintet és képesítés-érvényességet kezel.</div></div><button className="vir-link-button" onClick={()=>nav('/employees/skill-matrix')}>Teljes mátrix szerkesztése →</button></div></section></>}
{!loading&&tab==='simulator'&&<><section className="vir-panel"><div className="vir-panel-title">What-if paraméterek (%)</div><div className="vir-management-actions">{Object.entries(sim).map(([k,v])=><label className="vir-field" key={k}><span>{{revenue_change:'Árbevétel',price_change:'Ár',utilization_change:'Kihasználtság',wage_change:'Bérköltség',material_change:'Anyagköltség'}[k as keyof typeof sim]}</span><input type="number" value={v} onChange={e=>setSim(s=>({...s,[k]:Number(e.target.value)}))}/></label>)}</div></section>{data?.scenario&&<div className="vir-kpi-grid"><div className="vir-kpi"><div className="vir-kpi-label">Bázis árbevétel</div><div className="vir-kpi-value">{money(data.baseline.revenue)}</div></div><div className="vir-kpi"><div className="vir-kpi-label">Szimulált árbevétel</div><div className="vir-kpi-value">{money(data.scenario.revenue)}</div><div className="vir-kpi-note">Δ {money(data.delta.revenue)}</div></div><div className="vir-kpi"><div className="vir-kpi-label">Szimulált fedezet</div><div className="vir-kpi-value">{money(data.scenario.gross_profit)}</div><div className="vir-kpi-note">Δ {money(data.delta.gross_profit)}</div></div><div className={data.scenario.margin_percent>=35?'vir-kpi is-good':data.scenario.margin_percent>=20?'vir-kpi is-warning':'vir-kpi is-critical'}><div className="vir-kpi-label">Szimulált margin</div><div className="vir-kpi-value">{data.scenario.margin_percent}%</div></div></div>}</>}
</div>}
