import React,{useEffect,useMemo,useState}from'react';
import api from'../../api';

type Account={id:string;customer_id:string;balance:number;points:number;card_identifier?:string};
type PassRow={id:string;pass_type_name:string;service_id:string;remaining_quantity:number;valid_until?:string};
type Props={grossTotal:number;disabled?:boolean;onChange:(value:LoyaltySelection)=>void};
export type LoyaltySelection={accountId?:string;customerId?:string;walletAmount:number;pointsToSpend:number;couponCode:string;voucherCode:string;voucherAmount:number;passUsages:Array<{pass_id:string;service_id:string;quantity:number}>};
const money=(v:any)=>`${Number(v||0).toLocaleString('hu-HU')} Ft`;
export default function WorkOrderLoyaltyPanel({grossTotal,disabled,onChange}:Props){const[accounts,setAccounts]=useState<Account[]>([]);const[passes,setPasses]=useState<PassRow[]>([]);const[accountId,setAccountId]=useState('');const[walletAmount,setWalletAmount]=useState(0);const[pointsToSpend,setPointsToSpend]=useState(0);const[couponCode,setCouponCode]=useState('');const[voucherCode,setVoucherCode]=useState('');const[voucherAmount,setVoucherAmount]=useState(0);const[selectedPass,setSelectedPass]=useState('');const[message,setMessage]=useState('');const account=useMemo(()=>accounts.find(a=>a.id===accountId),[accounts,accountId]);const passRow=useMemo(()=>passes.find(p=>`${p.id}|${p.service_id}`===selectedPass),[passes,selectedPass]);useEffect(()=>{api.get('/api/transactions/loyalty/accounts').then(r=>setAccounts(Array.isArray(r.data)?r.data:[])).catch(()=>setAccounts([]))},[]);useEffect(()=>{setSelectedPass('');setPasses([]);if(!accountId)return;api.get(`/api/transactions/loyalty-operations/accounts/${accountId}/passes`).then(r=>setPasses(Array.isArray(r.data)?r.data:[])).catch(()=>setPasses([]))},[accountId]);useEffect(()=>{onChange({accountId:account?.id,customerId:account?.customer_id,walletAmount,pointsToSpend,couponCode:couponCode.trim(),voucherCode:voucherCode.trim(),voucherAmount,passUsages:passRow?[{pass_id:passRow.id,service_id:passRow.service_id,quantity:1}]:[]})},[account,walletAmount,pointsToSpend,couponCode,voucherCode,voucherAmount,passRow,onChange]);async function validateCoupon(){if(!couponCode.trim())return;try{const r=await api.get(`/api/transactions/loyalty/coupons/validate/${encodeURIComponent(couponCode.trim())}`);setMessage(r.data?.valid?'A kupon érvényes.':'A kupon nem használható.')}catch(e:any){setMessage(e?.response?.data?.message||'A kupon nem érvényes.')}}return <section style={box}><div style={{fontWeight:900,fontSize:18}}>Hűség és kedvezmények</div><div style={muted}>Wallet, pontok, bérlet, kupon és ajándékutalvány a munkalap lezárásához.</div><label style={field}>Vendég wallet<select disabled={disabled} value={accountId} onChange={e=>setAccountId(e.target.value)} style={input}><option value="">Nincs kiválasztva</option>{accounts.map(a=><option key={a.id} value={a.id}>{a.customer_id} · {money(a.balance)} · {Number(a.points||0).toLocaleString('hu-HU')} pont</option>)}</select></label>{account&&<div style={chips}><span>Egyenleg: <b>{money(account.balance)}</b></span><span>Pont: <b>{Number(account.points||0).toLocaleString('hu-HU')}</b></span></div>}<div style={grid}><label style={field}>Wallet felhasználás<input disabled={disabled||!account} type="number" min="0" max={Math.min(grossTotal,Number(account?.balance||0))} value={walletAmount} onChange={e=>setWalletAmount(Math.max(0,Number(e.target.value||0)))} style={input}/></label><label style={field}>Pont beváltás<input disabled={disabled||!account} type="number" min="0" max={Number(account?.points||0)} value={pointsToSpend} onChange={e=>setPointsToSpend(Math.max(0,Number(e.target.value||0)))} style={input}/></label></div>{account&&<label style={field}>Bérlet felhasználás<select disabled={disabled||!passes.length} value={selectedPass} onChange={e=>setSelectedPass(e.target.value)} style={input}><option value="">Nem használ bérletet</option>{passes.map(p=><option key={`${p.id}-${p.service_id}`} value={`${p.id}|${p.service_id}`}>{p.pass_type_name} · szolgáltatás {p.service_id} · {Number(p.remaining_quantity)} egység{p.valid_until?` · ${new Date(p.valid_until).toLocaleDateString('hu-HU')}`:''}</option>)}</select></label>}<div style={grid}><label style={field}>Kuponkód<div style={{display:'flex',gap:8}}><input disabled={disabled} value={couponCode} onChange={e=>setCouponCode(e.target.value)} style={input}/><button type="button" disabled={disabled||!couponCode.trim()} onClick={validateCoupon} style={btn}>Ellenőrzés</button></div></label><label style={field}>Ajándékutalvány kód<input disabled={disabled} value={voucherCode} onChange={e=>setVoucherCode(e.target.value)} style={input}/></label></div>{voucherCode.trim()&&<label style={field}>Utalványból felhasznált összeg<input disabled={disabled} type="number" min="0" max={grossTotal} value={voucherAmount} onChange={e=>setVoucherAmount(Math.max(0,Number(e.target.value||0)))} style={input}/></label>}{message&&<div style={{marginTop:10,fontSize:13,color:'#6941c6'}}>{message}</div>}</section>}
const box:React.CSSProperties={background:'#fff',borderRadius:18,padding:18,boxShadow:'0 8px 24px rgba(0,0,0,.08)',marginTop:18};const muted:React.CSSProperties={fontSize:13,color:'#667085',marginTop:4};const field:React.CSSProperties={display:'grid',gap:6,marginTop:14,fontSize:13,fontWeight:700};const input:React.CSSProperties={width:'100%',height:40,border:'1px solid #d0d5dd',borderRadius:10,padding:'0 10px',background:'#fff',boxSizing:'border-box'};const grid:React.CSSProperties={display:'grid',gridTemplateColumns:'1fr 1fr',gap:12};const chips:React.CSSProperties={display:'flex',gap:8,flexWrap:'wrap',marginTop:10};const btn:React.CSSProperties={border:'1px solid #d0d5dd',borderRadius:10,background:'#fff',padding:'0 12px',fontWeight:700};
