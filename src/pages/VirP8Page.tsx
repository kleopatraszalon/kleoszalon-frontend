import React,{useCallback,useEffect,useState}from'react';
import{getLocations,type LocationRow}from'../api/locations';
import{getP8ChannelOptimizer,getP8EmptySlotRecovery,getP8IdentityHub,getP8Inbox,getP8LayerStatus,getP8RevenueAttribution}from'../api/virP8';
import'./VirManagement.css';
type Tab='45'|'46'|'47'|'48'|'49'|'50';
const tabs:[Tab,string][]=[['45','Omnichannel Identity Hub'],['46','Universal Inbox'],['47','AI Receptionist 2.0'],['48','Empty Slot Recovery'],['49','Channel Optimizer'],['50','Conversation Revenue Attribution']];
const money=(v:unknown)=>new Intl.NumberFormat('hu-HU',{maximumFractionDigits:0}).format(Number(v)||0)+' Ft';
export default function VirP8Page(){const[locations,setLocations]=useState<LocationRow[]>([]),[locationId,setLocationId]=useState(''),[tab,setTab]=useState<Tab>('45'),[data,setData]=useState<any>(null),[status,setStatus]=useState<any>(null),[loading,setLoading]=useState(false),[error,setError]=useState('');useEffect(()=>{getLocations().then(r=>{setLocations(r);if(r.length)setLocationId(String(r[0].id))}).catch(()=>setLocations([]))},[]);useEffect(()=>{getP8LayerStatus({locationId:locationId||undefined}).then(setStatus).catch(()=>setStatus(null))},[locationId]);const load=useCallback(async()=>{setLoading(true);setError('');try{const p={locationId:locationId||undefined};let r:any;if(tab==='45')r=await getP8IdentityHub(p);else if(tab==='46')r=await getP8Inbox(p);else if(tab==='47')r=status||await getP8LayerStatus(p);else if(tab==='48')r=await getP8EmptySlotRecovery({...p,days:7});else if(tab==='49')r=await getP8ChannelOptimizer({...p,days:90});else r=await getP8RevenueAttribution({...p,days:90});setData(r)}catch(e:any){setError(e?.response?.data?.error||e?.message||'Betöltési hiba')}finally{setLoading(false)}},[tab,locationId,status]);useEffect(()=>{void load()},[load]);return <div className="vir-management-page"><div className="vir-management-header"><div><h1 className="vir-management-title">VIR P8 · Communication & Revenue Automation</h1><div className="vir-management-subtitle">Identity → Conversation → Handoff → Recovery → Channel optimization → Revenue attribution.</div></div><div className="vir-management-actions"><label className="vir-field"><span>Telephely</span><select value={locationId} onChange={e=>setLocationId(e.target.value)}><option value="">Összes telephely</option>{locations.map(l=><option key={l.id} value={l.id}>{l.name}</option>)}</select></label></div></div>{status&&<section className="vir-panel"><div className="vir-panel-title">Communication & Revenue Automation Layer</div><div className="vir-stat-line"><span>Identity Hub</span><strong>{status.capabilities?.identity_hub?'AKTÍV':'NEM AKTÍV'}</strong></div><div className="vir-stat-line"><span>Universal Inbox</span><strong>{status.capabilities?.universal_inbox?'AKTÍV':'NEM AKTÍV'}</strong></div><div className="vir-stat-line"><span>Consent + approval boundary</span><strong>{status.consent_aware&&status.approval_boundary?'AKTÍV':'NEM AKTÍV'}</strong></div><div className="vir-panel-muted">A csatornaoptimalizálás nem küld automatikusan üzenetet; külső provider csak érvényes kulccsal aktiválható.</div></section>}<div className="vir-tabs">{tabs.map(([id,label])=><button key={id} className={tab===id?'is-active':''} onClick={()=>setTab(id)}>{id}. {label}</button>)}</div>{error&&<div className="vir-error">{error}</div>}{loading&&<div className="vir-spinner">P8 számítása…</div>}{!loading&&data&&tab==='45'&&<section className="vir-panel"><div className="vir-panel-title">45. Omnichannel Identity Hub</div><div className="vir-panel-muted">Telefon/email mellett Viber subscriber ID, Messenger PSID, WhatsApp, push és webchat azonosítók egy vendéghez. Consent külön csatornán.</div><div className="vir-stat-line"><span>Azonosítók</span><strong>{data.items?.length||0}</strong></div><div className="vir-table-wrap"><table className="vir-table"><thead><tr><th>Vendég</th><th>Csatorna</th><th>Verified</th><th>Transactional</th><th>Marketing</th></tr></thead><tbody>{data.items?.slice(0,100).map((x:any)=><tr key={x.id}><td>{x.client_name}</td><td>{x.channel}</td><td>{String(x.verified)}</td><td>{String(x.transactional_consent)}</td><td>{String(x.marketing_consent)}</td></tr>)}</tbody></table></div></section>}{!loading&&data&&tab==='46'&&<section className="vir-panel"><div className="vir-panel-title">46. Universal Inbox</div><div className="vir-panel-muted">Viber, Messenger, WhatsApp, email, SMS, webchat, push és voice beszélgetések egy operátori nézetben.</div><div className="vir-stat-line"><span>Beszélgetések</span><strong>{data.items?.length||0}</strong></div>{data.items?.slice(0,30).map((x:any)=><div className="vir-stat-line" key={x.id}><span>{x.channel} · {x.client_name}</span><strong>{x.last_message||'—'}</strong></div>)}</section>}{!loading&&data&&tab==='47'&&<section className="vir-panel"><div className="vir-panel-title">47. AI Receptionist 2.0</div><div className="vir-panel-muted">Az AI a P6 booking-preview motorra épül; bizonytalan, panaszos vagy pénzügyi ügyet teljes summaryval emberhez ad át.</div><div className="vir-stat-line"><span>Nyitott human handoff</span><strong>{data.counts?.open_handoffs||0}</strong></div><div className="vir-stat-line"><span>Automatikus lezárás</span><strong>TILTVA</strong></div></section>}{!loading&&data&&tab==='48'&&<section className="vir-panel"><div className="vir-panel-title">48. Empty Slot Revenue Recovery</div><div className="vir-panel-muted">A P6 gap + waitlist motor üzleti akcióvá emelve. Nincs automatikus outreach vagy rebooking.</div><div className="vir-kpi-grid"><div className="vir-kpi"><div className="vir-kpi-label">Lehetőség</div><div className="vir-kpi-value">{data.summary?.opportunities||0}</div></div><div className="vir-kpi"><div className="vir-kpi-label">Menthető érték</div><div className="vir-kpi-value">{money(data.summary?.estimated_recoverable_value)}</div></div></div></section>}{!loading&&data&&tab==='49'&&<section className="vir-panel"><div className="vir-panel-title">49. Channel Optimizer</div><div className="vir-panel-muted">Consent + delivery + click + kommunikációs költség alapján vendégenként ajánlott csatorna. automatic_send=false.</div><div className="vir-stat-line"><span>Optimalizált vendégek</span><strong>{data.items?.length||0}</strong></div>{data.items?.slice(0,40).map((x:any)=><div className="vir-stat-line" key={x.client_id}><span>{x.client_name}</span><strong>{x.recommended_channel||'NINCS ENGEDÉLYEZETT CSATORNA'}</strong></div>)}</section>}{!loading&&data&&tab==='50'&&<section className="vir-panel"><div className="vir-panel-title">50. Conversation Revenue Attribution</div><div className="vir-panel-muted">14 napos last-touch booked-value attribúció. Nem állít kauzalitást, és jelenleg nem azonos a fizetett ledger-revenue-val.</div><div className="vir-table-wrap"><table className="vir-table"><thead><tr><th>Csatorna</th><th>Touch</th><th>Foglalás</th><th>Booked value</th><th>Költség</th><th>ROI proxy</th></tr></thead><tbody>{data.items?.map((x:any)=><tr key={x.channel}><td>{x.channel}</td><td>{x.touches}</td><td>{x.attributed_bookings}</td><td>{money(x.booked_value)}</td><td>{money(x.communication_cost)}</td><td>{x.roi_proxy??'—'}</td></tr>)}</tbody></table></div></section>}</div>}
