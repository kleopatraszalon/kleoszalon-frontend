import React,{useCallback,useEffect,useMemo,useState}from"react";import api from"../api/api";
const money=(v:any)=>`${Math.round(Number(v||0)).toLocaleString("hu-HU")} Ft`;
const dt=(v:any)=>v?new Date(v).toLocaleString("hu-HU",{month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit"}):"—";
const cycle=(name:string)=>{const n=String(name||"").toLowerCase();if(/gél|géllakk|manik|köröm|pedik/.test(n))return{min:21,max:28,label:"21–28 nap"};if(/hajfest|tőfest|balayage|melír|szők/.test(n))return{min:35,max:56,label:"35–56 nap"};if(/fodrász|hajvág|friz/.test(n))return{min:28,max:42,label:"28–42 nap"};if(/szempilla|szemöldök/.test(n))return{min:21,max:35,label:"21–35 nap"};if(/kozmetik|arckez|tisztít/.test(n))return{min:28,max:42,label:"28–42 nap"};if(/masszázs/.test(n))return{min:14,max:28,label:"14–28 nap"};return{min:45,max:70,label:"45–70 nap"}};
const CSS=String.raw`.bi{max-width:1540px;margin:0 auto;padding:0 26px 46px}.bih{display:flex;justify-content:space-between;align-items:end;margin:18px 0 12px}.bih h2{margin:0}.bih p{margin:4px 0;color:#746a64;font-size:12px}.bikpi{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:10px;margin-bottom:12px}.bik{background:#fff;border:1px solid #e8dfd8;border-radius:14px;padding:13px}.bik b{display:block;font-size:24px;margin-top:4px}.bigrid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.bic{background:#fff;border:1px solid #e8dfd8;border-radius:15px;padding:15px}.birow{display:grid;grid-template-columns:1.4fr .8fr .8fr auto;gap:8px;align-items:center;padding:9px 0;border-top:1px solid #eee8e2;font-size:12px}.tag{display:inline-flex;padding:3px 7px;border-radius:999px;background:#f3eee9;font-size:10px;font-weight:800}.danger{background:#ffe9e9}.warn{background:#fff2d6}.ok{background:#e9f7ed}.info{background:#eaf1ff}.bi button,.bi select{border:1px solid #ddd3cc;border-radius:9px;padding:8px 10px;background:#fff;font-weight:700}.binote{font-size:11px;color:#70655f;background:#f8f4f0;border-radius:9px;padding:9px}.command{grid-column:1/-1;border:2px solid #e2d4ca}.command h3{margin-bottom:5px}.cmdrow{display:grid;grid-template-columns:auto 1.5fr 1fr 1fr auto;gap:9px;align-items:center;padding:10px 0;border-top:1px solid #eee8e2;font-size:12px}.score{font-size:18px;font-weight:900}.loss{font-size:20px;font-weight:900}.policy{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:10px}.policy>div{background:#faf7f4;border-radius:10px;padding:10px;font-size:11px}@media(max-width:1100px){.bikpi{grid-template-columns:1fr 1fr}.bigrid{grid-template-columns:1fr}.birow,.cmdrow{grid-template-columns:1fr 1fr}.command{grid-column:auto}.policy{grid-template-columns:1fr}}`;
export default function BookingV4IntelligencePanel(){const[data,setData]=useState<any>({summary:{},no_show_risks:[],waitlist_matches:[],rebooking_candidates:[]}),[locations,setLocations]=useState<any[]>([]),[locationId,setLocationId]=useState(""),[error,setError]=useState("");const load=useCallback(async()=>{try{const[i,o]=await Promise.all([api.get(`/admin/booking-v4/intelligence${locationId?`?location_id=${locationId}`:""}`),api.get("/admin/booking-v4/overview?days=30")]);setData(i.data||{});setLocations(o.data?.locations||[]);setError("")}catch(e:any){setError(e?.response?.data?.error||e?.message||"Betöltési hiba")}},[locationId]);useEffect(()=>{void load()},[load]);
const high=useMemo(()=>data.no_show_risks?.filter((x:any)=>x.risk_level==='high')||[],[data]);
const command=useMemo(()=>{const items:any[]=[];(data.no_show_risks||[]).filter((x:any)=>x.risk_level!=='low').forEach((x:any)=>items.push({key:`risk-${x.id}`,score:x.risk_level==='high'?95:65,type:'Foglalási kockázat',title:x.client_name,detail:`${x.location_name} · ${dt(x.start_time)}`,impact:x.risk_level==='high'?'Előleg / megerősítés':'Emlékeztető',entity:x.id,location:x.location_id,payload:{risk_score:x.risk_score}}));(data.waitlist_matches||[]).forEach((x:any)=>{const hours=(new Date(x.start_time).getTime()-Date.now())/3600000;const score=Math.max(55,Math.min(99,Math.round(95-hours+Math.max(0,100-Number(x.priority||100))/10)));items.push({key:`wait-${x.waitlist_id}-${x.offer_id}`,score,type:'Várólista találat',title:x.client_name,detail:`${x.service_name} · ${dt(x.start_time)}`,impact:`${money(x.offer_price)} bevétel védhető`,entity:x.waitlist_id,waitlist:true,payload:{offer_id:x.offer_id,priority_score:score}})});(data.rebooking_candidates||[]).forEach((x:any)=>{const c=cycle(x.service_name),over=Math.max(0,Number(x.days_since_visit)-c.max);if(over>0)items.push({key:`re-${x.client_id}-${x.service_id}`,score:Math.min(90,55+over),type:'Visszahívás',title:x.client_name,detail:`${x.service_name} · ciklus ${c.label}`,impact:`${x.days_since_visit} napja járt`,entity:x.client_id,location:x.location_id,payload:{service_id:x.service_id,cycle:c.label,days_since_visit:x.days_since_visit}})});return items.sort((a,b)=>b.score-a.score).slice(0,30)},[data]);
const protectedRevenue=useMemo(()=>Math.round((data.waitlist_matches||[]).reduce((s:number,x:any)=>s+Number(x.offer_price||0),0)),[data]);
async function log(action_type:string,entity_type:string,entity_id:string|null,location_id:string|null,payload:any){await api.post("/admin/booking-v4/intelligence/actions",{action_type,entity_type,entity_id,location_id,payload});await load()}
async function markMatched(waitlistId:string){await api.patch(`/admin/booking-v4/waitlist/${waitlistId}`,{status:"matched"});await load()}
return <section className="bi"><style>{CSS}</style><div className="bih"><div><h2>Booking Intelligence & Command Center</h2><p>Napi teendők, no-show védelem, várólista, Last Minute, újrafoglalás és bevételvédelem egy helyen.</p></div><div><select value={locationId} onChange={e=>setLocationId(e.target.value)}><option value="">Minden szalon</option>{locations.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select> <button onClick={()=>void load()}>Frissítés</button></div></div>{error&&<div className="binote">{error}</div>}
<div className="bikpi"><div className="bik">Magas no-show<b>{data.summary?.high_no_show_risk||0}</b></div><div className="bik">Közepes kockázat<b>{data.summary?.medium_no_show_risk||0}</b></div><div className="bik">Aktív várólista<b>{data.summary?.waitlist_entries||0}</b></div><div className="bik">Azonnali találat<b>{data.summary?.waitlist_matches||0}</b></div><div className="bik">Újrafoglalható<b>{data.summary?.rebooking_candidates||0}</b></div><div className="bik">Védhető bevétel<b className="loss">{money(protectedRevenue)}</b></div></div>
<div className="bigrid"><div className="bic command"><h3>Mai teendők – Booking Command Center</h3><div className="binote">A lista üzleti prioritás szerint rendezi a beavatkozásokat. Először a közeli üres kapacitást és a magas no-show kockázatot kezeli, utána a visszahívást.</div>{!command.length&&<p>Nincs azonnali beavatkozást igénylő tétel.</p>}{command.map((x:any)=><div className="cmdrow" key={x.key}><span className={`tag ${x.score>=85?'danger':x.score>=65?'warn':'info'}`}>{x.type}</span><div><b>{x.title}</b><br/><small>{x.detail}</small></div><div className="score">{x.score} pont</div><div>{x.impact}</div>{x.waitlist?<button onClick={()=>void markMatched(x.entity)}>Találat elfogadása</button>:<button onClick={()=>void log("command_center_reviewed",x.type==='Visszahívás'?"client":"appointment",x.entity,x.location,x.payload)}>Intézve</button>}</div>)}</div>
<div className="bic"><h3>No-show kockázat és előlegpolitika</h3><div className="binote">A kockázat döntéstámogatás. A rendszer nem tilt automatikusan vendéget.</div>{!(data.no_show_risks||[]).length&&<p>Nincs közelgő foglalás.</p>}{(data.no_show_risks||[]).slice(0,30).map((x:any)=><div className="birow" key={x.id}><div><b>{x.client_name}</b><br/><small>{x.location_name} · {dt(x.start_time)}</small></div><div><span className={`tag ${x.risk_level==='high'?'danger':x.risk_level==='medium'?'warn':'ok'}`}>{x.risk_score}%</span></div><div>{x.risk_level==='high'?'Előleg + megerősítés':x.risk_level==='medium'?'Automatikus emlékeztető':'Normál folyamat'}</div><button onClick={()=>void log("risk_reviewed","appointment",x.id,x.location_id,{risk_score:x.risk_score,deposit_policy:x.risk_level==='high'?'deposit_and_confirmation':x.risk_level==='medium'?'reminder':'normal'})}>Ellenőrizve</button></div>)}</div>
<div className="bic"><h3>Várólista × Last Minute</h3><div className="binote">Last Minute kedvezmény előtt először várólista-match: azonos szalon + szolgáltatás + időablak + szakemberpreferencia.</div>{!(data.waitlist_matches||[]).length&&<p>Nincs automatikus találat.</p>}{(data.waitlist_matches||[]).slice(0,30).map((x:any)=>{const hours=(new Date(x.start_time).getTime()-Date.now())/3600000,score=Math.max(55,Math.min(99,Math.round(95-hours+Math.max(0,100-Number(x.priority||100))/10)));return <div className="birow" key={`${x.waitlist_id}-${x.offer_id}`}><div><b>{x.client_name}</b><br/><small>{x.service_name} · {x.location_name}</small></div><div>{dt(x.start_time)}<br/><span className="tag info">prioritás {score}</span></div><div>{money(x.offer_price)} · -{x.discount_percent}%</div><button onClick={()=>void markMatched(x.waitlist_id)}>Elfogadás</button></div>})}</div>
<div className="bic"><h3>Dinamikus újrafoglalási ciklus</h3><div className="binote">Nem minden szolgáltatás kap azonos 45–70 napos szabályt: a javaslat a szolgáltatás természetes visszatérési ciklusához igazodik.</div>{!(data.rebooking_candidates||[]).length&&<p>Nincs jelölt.</p>}{(data.rebooking_candidates||[]).slice(0,30).map((x:any)=>{const c=cycle(x.service_name),due=x.days_since_visit>=c.max;return <div className="birow" key={`${x.client_id}-${x.service_id}`}><div><b>{x.client_name}</b><br/><small>{x.service_name} · {x.location_name}</small></div><div>{x.days_since_visit} napja</div><div><span className={`tag ${due?'warn':'ok'}`}>{c.label} · {due?'esedékes':'figyelendő'}</span></div><button onClick={()=>void log("rebooking_candidate_reviewed","client",x.client_id,x.location_id,{service_id:x.service_id,last_visit:x.last_visit,cycle:c.label,due})}>Megjelölés</button></div>})}</div>
<div className="bic"><h3>Bevételvédelem és működési szabályok</h3><p className="binote">A jelenlegi várólista-találatokból közvetlenül védhető becsült bevétel: <b>{money(protectedRevenue)}</b>. Ez nem könyvelt árbevétel, hanem az aktuális ajánlati árak összege.</p><div className="policy"><div><b>1. Várólista-first</b><br/>Last Minute kedvezmény előtt mindig meglévő várólista ellenőrzése.</div><div><b>2. Kockázatalapú előleg</b><br/>Csak magas no-show kockázatnál javasolt; közepesnél emlékeztető.</div><div><b>3. Dinamikus ciklus</b><br/>Köröm 21–28, hajfestés 35–56, hajvágás 28–42 nap; szolgáltatásfüggő visszahívás.</div></div><p><b>Aktuális magas kockázat:</b> {high.length} foglalás · <b>Mai prioritásos teendő:</b> {command.length} tétel.</p></div></div></section>};