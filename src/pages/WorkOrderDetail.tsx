import React,{useCallback,useEffect,useMemo,useState}from'react';
import{ArrowLeft,CheckCircle2,ClipboardCheck,LockKeyhole,RefreshCw,Save}from'lucide-react';
import{useNavigate,useParams}from'react-router-dom';
import{apiFetch}from'../utils/api';

type WorkOrderItem={id:string;item_type?:string;item_name?:string;quantity?:number|string;unit_price?:number|string;discount_amount?:number|string;line_total?:number|string;duration_minutes?:number|string};
type Payment={id:string;payment_method?:string;amount?:number|string;paid_at?:string;note?:string};
type WorkOrder={id:string;work_order_number?:string;title?:string;notes?:string;status?:string;created_at?:string;updated_at?:string;started_at?:string;completed_at?:string;locked_at?:string|null;locked_reason?:string|null;archived_at?:string|null;location_name?:string|null;client_name?:string|null;client_phone?:string|null;client_email?:string|null;employee_name?:string|null;fully_paid?:boolean;note_for_another_visitor?:boolean;can_edit?:boolean;items?:WorkOrderItem[];payments?:Payment[];total_gross?:number|string;gross_total?:number|string;total?:number|string};
type Draft={title:string;notes:string;client_name:string;client_phone:string;client_email:string;fully_paid:boolean;note_for_another_visitor:boolean};

const STATUS_OPTIONS=[['waiting','Várakozik'],['arrived','Megérkezett'],['in_progress','Folyamatban'],['completed','Lezárt'],['cancelled','Lemondott'],['no_show','Nem jelent meg']] as const;
const statusHu=(s?:string)=>STATUS_OPTIONS.find(([key])=>key===s)?.[1]||s||'—';
const money=(v:unknown)=>`${Number(v||0).toLocaleString('hu-HU',{maximumFractionDigits:0})} Ft`;
const dateTime=(v?:string|null)=>v?new Date(v).toLocaleString('hu-HU'):'—';
const emptyDraft:Draft={title:'',notes:'',client_name:'',client_phone:'',client_email:'',fully_paid:false,note_for_another_visitor:false};

export default function WorkOrderDetail(){
  const{id}=useParams();const navigate=useNavigate();const[data,setData]=useState<WorkOrder|null>(null);const[draft,setDraft]=useState<Draft>(emptyDraft);const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[statusSaving,setStatusSaving]=useState(false);const[error,setError]=useState('');const[notice,setNotice]=useState('');
  const load=useCallback(async()=>{if(!id)return;setLoading(true);setError('');try{const result=await apiFetch<WorkOrder>(`/api/workorders/${encodeURIComponent(id)}`);setData(result);setDraft({title:String(result?.title||''),notes:String(result?.notes||''),client_name:String(result?.client_name||''),client_phone:String(result?.client_phone||''),client_email:String(result?.client_email||''),fully_paid:Boolean(result?.fully_paid),note_for_another_visitor:Boolean(result?.note_for_another_visitor)})}catch(e:any){setError(e?.message||'A munkalap nem tölthető be.')}finally{setLoading(false)}},[id]);
  useEffect(()=>{void load()},[load]);
  const locked=Boolean(data?.locked_at||data?.archived_at);const canEdit=Boolean(data?.can_edit)&&!locked;
  const itemTotal=useMemo(()=>Number(data?.items?.reduce((sum,item)=>sum+Number(item.line_total||0),0)||data?.total_gross||data?.gross_total||data?.total||0),[data]);
  const paidTotal=useMemo(()=>Number(data?.payments?.reduce((sum,p)=>sum+Number(p.amount||0),0)||0),[data?.payments]);
  const save=async()=>{if(!id||!canEdit)return;if(!draft.title.trim()){setError('A munkalap címe nem lehet üres.');return}setSaving(true);setError('');setNotice('');try{await apiFetch(`/api/workorders/${encodeURIComponent(id)}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({...draft,title:draft.title.trim()})});setNotice('A munkalap adatai mentve.');await load()}catch(e:any){setError(e?.message||'A munkalap módosítása nem sikerült.')}finally{setSaving(false)}};
  const changeStatus=async(status:string)=>{if(!id||!canEdit||status===data?.status)return;setStatusSaving(true);setError('');setNotice('');try{await apiFetch(`/api/workorders/${encodeURIComponent(id)}/lifecycle`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status})});setNotice(`Státusz módosítva: ${statusHu(status)}.`);await load()}catch(e:any){setError(e?.message||'A státusz módosítása nem sikerült.')}finally{setStatusSaving(false)}};
  if(loading&&!data)return <main style={page}><div style={state}><RefreshCw className="spin"/> Munkalap betöltése…</div></main>;
  if(error&&!data)return <main style={page}><button style={back} onClick={()=>navigate('/workorders')}><ArrowLeft size={16}/> Vissza</button><div style={errorBox}>{error}</div></main>;
  if(!data)return null;
  return <main style={page}>
    <header style={hero}><div><button style={back} onClick={()=>navigate('/workorders')}><ArrowLeft size={16}/> Munkalapok</button><span style={eyebrow}>MUNKALAP</span><h1 style={{margin:'4px 0 0'}}>{data.work_order_number||data.id}</h1><p style={muted}>{data.location_name||'Nincs szalon megadva'} · {statusHu(data.status)}</p></div><div style={{display:'flex',gap:8,alignItems:'center',flexWrap:'wrap'}}>{locked?<span style={lockedPill}><LockKeyhole size={14}/> LEZÁRT · ARCHIVÁLT</span>:canEdit?<span style={editPill}><CheckCircle2 size={14}/> SZERKESZTHETŐ</span>:<span style={readPill}>CSAK OLVASHATÓ</span>}<button style={secondary} onClick={()=>void load()}><RefreshCw size={16}/> Frissítés</button></div></header>
    {error&&<div style={errorBox}>{error}</div>}{notice&&<div style={successBox}>{notice}</div>}
    <section style={grid}>
      <article style={panel}><div style={panelTitle}><ClipboardCheck size={19}/> Alapadatok</div><label style={field}>Munkalap címe<input style={input} value={draft.title} disabled={!canEdit||saving} onChange={e=>setDraft(v=>({...v,title:e.target.value}))}/></label><label style={field}>Megjegyzés<textarea style={{...input,height:115,paddingTop:10}} value={draft.notes} disabled={!canEdit||saving} onChange={e=>setDraft(v=>({...v,notes:e.target.value}))}/></label><div style={two}><label style={field}>Létrehozva<input style={input} value={dateTime(data.created_at)} disabled/></label><label style={field}>Módosítva<input style={input} value={dateTime(data.updated_at)} disabled/></label></div>{locked&&<div style={archiveBox}><b>Archivált munkalap</b><span>Lezárva: {dateTime(data.locked_at||data.archived_at)}</span>{data.locked_reason&&<span>Ok: {data.locked_reason}</span>}</div>}</article>
      <article style={panel}><div style={panelTitle}>Vendég</div><label style={field}>Név<input style={input} value={draft.client_name} disabled={!canEdit||saving} onChange={e=>setDraft(v=>({...v,client_name:e.target.value}))}/></label><label style={field}>Telefonszám<input style={input} value={draft.client_phone} disabled={!canEdit||saving} onChange={e=>setDraft(v=>({...v,client_phone:e.target.value}))}/></label><label style={field}>E-mail<input style={input} value={draft.client_email} disabled={!canEdit||saving} onChange={e=>setDraft(v=>({...v,client_email:e.target.value}))}/></label><label style={check}><input type="checkbox" checked={draft.fully_paid} disabled={!canEdit||saving} onChange={e=>setDraft(v=>({...v,fully_paid:e.target.checked}))}/> Teljesen kifizetve</label><label style={check}><input type="checkbox" checked={draft.note_for_another_visitor} disabled={!canEdit||saving} onChange={e=>setDraft(v=>({...v,note_for_another_visitor:e.target.checked}))}/> Bejegyzés egy másik látogató számára</label></article>
      <article style={panel}><div style={panelTitle}>Állapot és összesítő</div><label style={field}>Státusz<select style={input} value={data.status||''} disabled={!canEdit||statusSaving} onChange={e=>void changeStatus(e.target.value)}>{STATUS_OPTIONS.map(([key,label])=><option key={key} value={key}>{label}</option>)}</select></label><div style={summary}><div><span>Szalon</span><b>{data.location_name||'—'}</b></div><div><span>Munkatárs</span><b>{data.employee_name||'—'}</b></div><div><span>Tételek értéke</span><b>{money(itemTotal)}</b></div><div><span>Rögzített fizetés</span><b>{money(paidTotal)}</b></div><div><span>Kezdés</span><b>{dateTime(data.started_at)}</b></div><div><span>Befejezés</span><b>{dateTime(data.completed_at)}</b></div></div>{canEdit&&<button style={primary} disabled={saving} onClick={()=>void save()}><Save size={16}/>{saving?'Mentés…':'Módosítások mentése'}</button>}</article>
    </section>
    <section style={panel}><div style={panelTitle}>Szolgáltatások és termékek</div><div style={{overflowX:'auto'}}><table style={table}><thead><tr><th style={th}>Típus</th><th style={th}>Megnevezés</th><th style={th}>Mennyiség</th><th style={th}>Egységár</th><th style={th}>Kedvezmény</th><th style={th}>Összesen</th></tr></thead><tbody>{(data.items||[]).map(item=><tr key={item.id}><td style={td}>{item.item_type==='service'?'Szolgáltatás':item.item_type==='product'?'Termék':item.item_type||'—'}</td><td style={td}><b>{item.item_name||'—'}</b>{item.duration_minutes?<small style={{display:'block',...muted}}>{item.duration_minutes} perc</small>:null}</td><td style={td}>{Number(item.quantity||0).toLocaleString('hu-HU')}</td><td style={td}>{money(item.unit_price)}</td><td style={td}>{money(item.discount_amount)}</td><td style={td}><b>{money(item.line_total)}</b></td></tr>)}{!data.items?.length&&<tr><td colSpan={6} style={{...td,textAlign:'center'}}>Nincs rögzített tétel.</td></tr>}</tbody></table></div></section>
    <section style={panel}><div style={panelTitle}>Fizetések</div><div style={{overflowX:'auto'}}><table style={table}><thead><tr><th style={th}>Mód</th><th style={th}>Összeg</th><th style={th}>Időpont</th><th style={th}>Megjegyzés</th></tr></thead><tbody>{(data.payments||[]).map(p=><tr key={p.id}><td style={td}>{p.payment_method||'—'}</td><td style={td}><b>{money(p.amount)}</b></td><td style={td}>{dateTime(p.paid_at)}</td><td style={td}>{p.note||'—'}</td></tr>)}{!data.payments?.length&&<tr><td colSpan={4} style={{...td,textAlign:'center'}}>Nincs külön fizetési tétel rögzítve.</td></tr>}</tbody></table></div></section>
  </main>;
}

const page:React.CSSProperties={padding:24,background:'#f7f4f0',minHeight:'100vh'};const state:React.CSSProperties={padding:24,display:'flex',gap:8,alignItems:'center'};const hero:React.CSSProperties={display:'flex',justifyContent:'space-between',gap:18,alignItems:'center',marginBottom:16};const eyebrow:React.CSSProperties={display:'block',fontSize:11,fontWeight:900,letterSpacing:1.5,color:'#8a6d55',marginTop:10};const muted:React.CSSProperties={fontSize:12,color:'#74675d',margin:'4px 0'};const grid:React.CSSProperties={display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(280px,1fr))',gap:16,marginBottom:16};const panel:React.CSSProperties={background:'#fff',borderRadius:16,padding:18,boxShadow:'0 8px 24px rgba(48,36,25,.08)',marginBottom:16};const panelTitle:React.CSSProperties={display:'flex',alignItems:'center',gap:8,fontSize:18,fontWeight:900,marginBottom:8};const field:React.CSSProperties={display:'grid',gap:6,marginTop:12,fontSize:12,fontWeight:800,color:'#54483f'};const input:React.CSSProperties={width:'100%',height:40,border:'1px solid #d8cec4',borderRadius:9,padding:'0 11px',boxSizing:'border-box',background:'#fff'};const two:React.CSSProperties={display:'grid',gridTemplateColumns:'1fr 1fr',gap:10};const check:React.CSSProperties={display:'flex',gap:8,alignItems:'center',marginTop:13,fontSize:13};const summary:React.CSSProperties={display:'grid',gap:0,border:'1px solid #ece3da',borderRadius:11,overflow:'hidden',marginTop:12};const primary:React.CSSProperties={display:'flex',alignItems:'center',justifyContent:'center',gap:7,width:'100%',marginTop:14,border:0,borderRadius:9,padding:'10px 12px',background:'#2b2118',color:'#fff',fontWeight:900,cursor:'pointer'};const secondary:React.CSSProperties={display:'flex',alignItems:'center',gap:6,border:'1px solid #d8cec4',borderRadius:9,padding:'9px 11px',background:'#fff',fontWeight:800,cursor:'pointer'};const back:React.CSSProperties={...secondary,padding:'7px 9px'};const pill:React.CSSProperties={display:'flex',alignItems:'center',gap:5,borderRadius:999,padding:'6px 9px',fontSize:11,fontWeight:900};const editPill:React.CSSProperties={...pill,background:'#ecfdf3',color:'#027a48'};const readPill:React.CSSProperties={...pill,background:'#f2f4f7',color:'#475467'};const lockedPill:React.CSSProperties={...pill,background:'#fef3f2',color:'#b42318'};const errorBox:React.CSSProperties={padding:12,borderRadius:10,background:'#fef3f2',color:'#b42318',marginBottom:14};const successBox:React.CSSProperties={padding:12,borderRadius:10,background:'#ecfdf3',color:'#027a48',marginBottom:14};const archiveBox:React.CSSProperties={display:'grid',gap:4,marginTop:14,padding:12,borderRadius:10,background:'#fef3f2',color:'#7a271a',fontSize:12};const table:React.CSSProperties={width:'100%',borderCollapse:'collapse',fontSize:13};const th:React.CSSProperties={textAlign:'left',padding:'9px 7px',borderBottom:'1px solid #e9e1d9',whiteSpace:'nowrap'};const td:React.CSSProperties={padding:'10px 7px',borderTop:'1px solid #eee8e2',verticalAlign:'top'};
