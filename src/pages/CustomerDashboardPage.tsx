import React,{useEffect,useMemo,useState}from'react';import{CalendarDays,CalendarPlus,ChevronRight,Clock3,Gift,MapPin,Percent,Sparkles,TicketCheck,WalletCards}from'lucide-react';import{useNavigate}from'react-router-dom';import api from'../api/api';import WorkOrderDashboardWidget from'./dashboard/WorkOrderDashboardWidget';import'./CustomerDashboardPage.css';
type Pass={id:string;pass_type_name:string;valid_until?:string|null;remaining_units:number|string;original_units:number|string;services?:Array<{service_name:string;remaining_quantity:number|string}>};type Discount={campaign_id:string;name:string;discount_type:string;discount_value:number|string;valid_until?:string|null;code?:string|null};type Promotion={kind:'discount'|'service'|string;title:string;discount_type?:string;discount_value?:number;valid_until?:string|null;code?:string|null;regular_price?:number;promo_price?:number;category_name?:string};type Appointment={id:string;title:string;start_time:string;end_time:string;status:string;employee_name?:string|null;location_name?:string|null};type Data={customer:{full_name:string;email?:string|null};account:{balance:number;points:number;card_identifier?:string|null};passes:Pass[];discounts:Discount[];promotions:Promotion[];upcoming_appointments:Appointment[]};const money=(v:unknown)=>`${Number(v||0).toLocaleString('hu-HU',{maximumFractionDigits:0})} Ft`;const date=(v?:string|null)=>v?new Date(v).toLocaleDateString('hu-HU',{year:'numeric',month:'short',day:'numeric'}):'Nincs lejárat';const datetime=(v:string)=>new Date(v).toLocaleString('hu-HU',{month:'short',day:'numeric',hour:'2-digit',minute:'2-digit'});
export default function CustomerDashboardPage(){const navigate=useNavigate();const[data,setData]=useState<Data|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState('');useEffect(()=>{let active=true;setLoading(true);api.get<Data>('/customer-portal/dashboard').then(r=>{if(active)setData(r.data)}).catch(e=>{if(active)setError(e?.response?.data?.error||e?.message||'Az ügyféladatok nem tölthetők be.')}).finally(()=>{if(active)setLoading(false)});return()=>{active=false}},[]);const activePasses=data?.passes?.length||0,discountCount=data?.discounts?.length||0;const headline=useMemo(()=>data?.customer?.full_name?`Üdvözlünk, ${data.customer.full_name}!`:'Üdvözlünk!',[data?.customer?.full_name]);if(loading)return <main className="customer-dashboard"><div className="customer-loading">Ügyfélfiók betöltése…</div></main>;if(error)return <main className="customer-dashboard"><div className="customer-error">{error}</div></main>;if(!data)return null;return <main className="customer-dashboard"><header className="customer-hero"><div><span>SAJÁT ÜGYFÉLFIÓK</span><h1>{headline}</h1><p>Itt látod az egyenlegedet, bérleteidet, kedvezményeidet, időpontjaidat és a saját munkalapjaidat.</p></div><button onClick={()=>navigate('/customer/booking')}><CalendarPlus/> Új bejelentkezés</button></header><WorkOrderDashboardWidget/><section className="customer-kpis"><article><WalletCards/><div><small>Felhasználható egyenleg</small><strong>{money(data.account.balance)}</strong><span>{data.account.card_identifier||'Kleopátra ügyfélegyenleg'}</span></div></article><article><Sparkles/><div><small>Hűségpontok</small><strong>{Number(data.account.points||0).toLocaleString('hu-HU')}</strong><span>Aktuális pontállomány</span></div></article><article><TicketCheck/><div><small>Aktív bérletek</small><strong>{activePasses}</strong><span>{activePasses?'Felhasználható bérletek':'Nincs aktív bérlet'}</span></div></article><article><Percent/><div><small>Kedvezmények</small><strong>{discountCount}</strong><span>{discountCount?'Jelenleg elérhető':'Nincs aktív kedvezmény'}</span></div></article></section><section className="customer-grid"><article className="customer-panel customer-next"><header><div><span>KÖVETKEZŐ IDŐPONTOK</span><h2>Bejelentkezéseim</h2></div><button onClick={()=>navigate('/customer/booking')}>Új időpont <ChevronRight/></button></header>{data.upcoming_appointments?.length?<div className="customer-appointment-list">{data.upcoming_appointments.map(a=><div key={a.id}><CalendarDays/><div><b>{a.title}</b><span><Clock3/>{datetime(a.start_time)}{a.employee_name?` · ${a.employee_name}`:''}</span>{a.location_name&&<small><MapPin/>{a.location_name}</small>}</div><em>{a.status}</em></div>)}</div>:<div className="customer-empty">Nincs közelgő időpontod.</div>}</article><article className="customer-panel"><header><div><span>BÉRLETEK</span><h2>Aktív bérleteim</h2></div><Gift/></header>{data.passes?.length?<div className="customer-pass-list">{data.passes.map(p=><div key={p.id}><div><b>{p.pass_type_name}</b><span>Érvényes: {date(p.valid_until)}</span></div><strong>{Number(p.remaining_units||0).toLocaleString('hu-HU')}<small> / {Number(p.original_units||0).toLocaleString('hu-HU')} egység</small></strong>{p.services?.length?<p>{p.services.map(s=>`${s.service_name}: ${Number(s.remaining_quantity||0).toLocaleString('hu-HU')}`).join(' · ')}</p>:null}</div>)}</div>:<div className="customer-empty">Jelenleg nincs aktív bérleted.</div>}</article></section><section className="customer-grid"><article className="customer-panel"><header><div><span>KEDVEZMÉNYEIM</span><h2>Aktív kedvezmények</h2></div><Percent/></header>{data.discounts?.length?<div className="customer-discount-list">{data.discounts.map(d=><div key={`${d.campaign_id}-${d.code||'global'}`}><span>{d.discount_type==='percent'?`${Number(d.discount_value)}%`:money(d.discount_value)}</span><div><b>{d.name}</b><small>{d.code?`Kód: ${d.code} · `:''}Érvényes: {date(d.valid_until)}</small></div></div>)}</div>:<div className="customer-empty">Nincs aktív személyes kedvezményed.</div>}</article><article className="customer-panel customer-promos"><header><div><span>AKTUÁLIS AKCIÓK</span><h2>Most érdemes kihasználni</h2></div><Sparkles/></header>{data.promotions?.length?<div className="customer-promo-scroll">{data.promotions.map((p,i)=><div key={`${p.kind}-${p.title}-${i}`}><b>{p.title}</b>{p.kind==='service'?<><strong>{money(p.promo_price)}</strong><small>{p.regular_price?`Eredeti ár: ${money(p.regular_price)}`:'Akciós szolgáltatás'}</small></>:<><strong>{p.discount_type==='percent'?`${Number(p.discount_value||0)}% kedvezmény`:money(p.discount_value)}</strong><small>{p.code?`Kód: ${p.code}`:`Érvényes: ${date(p.valid_until)}`}</small></>}</div>)}</div>:<div className="customer-empty">Jelenleg nincs kiemelt akció.</div>}</article></section></main>}
