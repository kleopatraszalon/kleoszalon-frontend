import React,{useEffect,useState} from 'react';
import {approveP15ActionPlan,getP15ActionPlans,getP15CommandCenter,getP15Status,previewP15ActionPlan} from '../api/virP15';
export default function VirP15Page(){const [data,setData]=useState<any>(null);const [status,setStatus]=useState<any>(null);const [title,setTitle]=useState('Nyitott vezetői akció');const [reason,setReason]=useState('VIR jelzés alapján kontrollált beavatkozás szükséges.');const [loading,setLoading]=useState(false);useEffect(()=>{void getP15Status().then(setStatus)},[]);const run=async(kind:'command'|'list'|'preview')=>{setLoading(true);try{setData(kind==='command'?await getP15CommandCenter():kind==='list'?await getP15ActionPlans():await previewP15ActionPlan({title,reason,action_type:'MANAGEMENT_FOLLOWUP',proposal:{source:'P15_UI'}}))}finally{setLoading(false)}};const approve=async()=>{const id=String(data?.item?.id||data?.items?.[0]?.id||'');if(!id)return;setData(await approveP15ActionPlan(id))};return <div style={{padding:24,maxWidth:1500,margin:'0 auto'}}><h1>VIR P15 · Executive Autopilot & Governance 4.0</h1><p>69. Executive Command Center · 70. Controlled Action Plans · 71. Approval Register · 72. Governance Audit. Jóváhagyás után sincs automatikus végrehajtás.</p>{status&&<p>Preview tervek: <strong>{status.counts?.preview_plans??0}</strong> · Jóváhagyott: <strong>{status.counts?.approved_plans??0}</strong></p>}<div style={{display:'flex',gap:8,flexWrap:'wrap',marginBottom:10}}><button onClick={()=>void run('command')} disabled={loading}>Command center</button><button onClick={()=>void run('list')} disabled={loading}>Action plans</button></div><input value={title} onChange={e=>setTitle(e.target.value)} style={{minWidth:320}}/><textarea value={reason} onChange={e=>setReason(e.target.value)} rows={3} style={{display:'block',width:'100%',margin:'8px 0'}}/><button onClick={()=>void run('preview')} disabled={loading||!title||!reason}>Action plan preview</button><button onClick={()=>void approve()} disabled={loading}>Preview jóváhagyása</button><p><strong>Kontroll:</strong> az approval kizárólag auditált státuszváltás; execution_enabled=false.</p><pre style={{whiteSpace:'pre-wrap',padding:16,border:'1px solid #ddd',borderRadius:12}}>{data?JSON.stringify(data,null,2):'Nincs adat.'}</pre></div>}
