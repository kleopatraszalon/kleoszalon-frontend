import fs from'fs';import path from'path';
const page=fs.readFileSync(path.join(__dirname,'KnowledgeBasePage.tsx'),'utf8');const customizer=fs.readFileSync(path.join(__dirname,'VirCustomizerPage.tsx'),'utf8');const runtime=fs.readFileSync(path.join(__dirname,'../components/VirCustomizationRuntime.tsx'),'utf8');const utility=fs.readFileSync(path.join(__dirname,'../utils/virCustomization.ts'),'utf8');
test('knowledge base renders sources, role quiz review and management customizer',()=>{expect(page).toContain('Forrásolt szakmai tudástár');expect(page).toContain('Munkaköri teszt');expect(page).toContain('result.review');expect(page).toContain('VIR Admin');expect(page).toContain('VirCustomizerPage');expect(page).toContain('hasStoredRole(["admin","manager"])');expect(customizer).toContain('/transactions/knowledge-base/vir-customizer');expect(customizer).toContain('Auditnapló')});
test('runtime customization applies brand variables and persists cached config',()=>{expect(runtime).toContain('applyVirCustomization');expect(runtime).toContain('/transactions/knowledge-base/vir-customizer');expect(utility).toContain('--color-magenta');expect(utility).toContain('--radius-md');expect(utility).toContain('kleo.vir.customization');expect(utility).toContain('document.title')});
