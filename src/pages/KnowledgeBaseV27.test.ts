import fs from'fs';import path from'path';
const page=fs.readFileSync(path.join(__dirname,'KnowledgeBasePage.tsx'),'utf8');const runtime=fs.readFileSync(path.join(__dirname,'../components/VirCustomizationRuntime.tsx'),'utf8');
test('knowledge base renders sources, role quiz review and management customizer',()=>{expect(page).toContain('Forrásolt szakmai tudástár');expect(page).toContain('Munkaköri teszt');expect(page).toContain('result.review');expect(page).toContain('VIR Admin');expect(page).toContain('vir-customizer');expect(page).toContain('hasStoredRole(["admin","manager"])')});
test('runtime customization applies brand variables and persists cached config',()=>{expect(runtime).toContain('--color-magenta');expect(runtime).toContain('--radius-md');expect(runtime).toContain('kleo.vir.customization');expect(runtime).toContain('document.title');expect(runtime).toContain("api.get('/transactions/knowledge-base/vir-customizer')")});
