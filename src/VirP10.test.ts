import fs from 'node:fs';import {describe,it,expect} from 'vitest';
describe('VIR P10 Revenue Autopilot UI',()=>{const routes=fs.readFileSync('src/routing/adminRoutes.tsx','utf8');const pages=fs.readFileSync('src/routing/routePages.ts','utf8');const api=fs.readFileSync('src/api/virP10.ts','utf8');const page=fs.readFileSync('src/pages/VirP10Page.tsx','utf8');it('registers management route',()=>{expect(routes).toContain('/admin/vir/p10');expect(routes).toContain('R(MANAGEMENT, <VirP10Page />)');expect(pages).toContain('VirP10Page')});it('covers P10 APIs',()=>['dynamic-offers','empty-slot-autopilot','revenue-guard','next-best-offers','promotion-simulator'].forEach(x=>expect(api).toContain(x)));it('shows all five modules and approval boundary',()=>{['51. Dynamic Offer Engine','52. Empty Slot Autopilot 2.0','53. Revenue Guard','54. Next Best Offer','55. Promotion Profit Simulator'].forEach(x=>expect(page).toContain(x));expect(page).toContain('jóváhagyást igényel')})});
