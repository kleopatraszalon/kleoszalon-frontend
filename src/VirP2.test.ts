import fs from 'fs';
const page=fs.readFileSync('src/pages/VirP2Page.tsx','utf8');const api=fs.readFileSync('src/api/virP2.ts','utf8');const routes=fs.readFileSync('src/routing/adminRoutes.tsx','utf8');const cockpit=fs.readFileSync('src/pages/VirManagerCockpitPage.tsx','utf8');
test('P2 exposes all four requested modules',()=>{expect(page).toContain('VIR AI Copilot');expect(page).toContain('Anomáliafelismerés');expect(page).toContain('Automatikus vezetői összefoglalók');expect(page).toContain('Szalonok közötti benchmarking')});
test('P2 uses governed relative VIR APIs',()=>{expect(api).toContain('/vir/p2/copilot/ask');expect(api).toContain('/vir/p2/anomalies');expect(api).toContain('/vir/p2/summaries');expect(api).toContain('/vir/p2/benchmark');expect(api).not.toContain('onrender.com')});
test('P2 route is management protected and linked from cockpit',()=>{expect(routes).toContain('/admin/vir/p2');expect(routes).toContain('R(MANAGEMENT, <VirP2Page />)');expect(cockpit).toContain('VIR Intelligence P2')});
