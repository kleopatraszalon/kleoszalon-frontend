self.addEventListener('install',()=>self.skipWaiting());self.addEventListener('activate',e=>e.waitUntil(self.clients.claim()));self.addEventListener('push',e=>{let d={};try{d=e.data.json()}catch{}e.waitUntil(self.registration.showNotification(d.title||'Kleopátra értesítés',{body:d.body||'Új értesítés érkezett.',icon:'/logo192.png',badge:'/logo192.png',image:d.image||undefined,tag:d.tag||'kleopatra-notification',renotify:true,vibrate:d.severity==='critical'?[220,80,220,80,220]:[180,80,180],data:{url:d.url||'/dashboard/notifications'},actions:[{action:'open',title:'Megnyitás'}]}))});self.addEventListener('notificationclick',e=>{e.notification.close();e.waitUntil((async()=>{const url=e.notification.data?.url||'/dashboard/notifications',windows=await clients.matchAll({type:'window',includeUncontrolled:true}),existing=windows.find(w=>new URL(w.url).pathname===new URL(url,self.location.origin).pathname);if(existing){await existing.focus();return existing.navigate(url)}return clients.openWindow(url)})())});
