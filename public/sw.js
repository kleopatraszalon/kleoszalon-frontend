self.addEventListener('install',()=>self.skipWaiting());self.addEventListener('activate',e=>e.waitUntil(self.clients.claim()));self.addEventListener('push',e=>{let d={};try{d=e.data.json()}catch{}e.waitUntil(self.registration.showNotification(d.title||'Kleopátra Beauty Club',{body:d.body||'Új napi ajánlat érkezett.',icon:'/logo192.png',badge:'/logo192.png',image:d.image||undefined,tag:d.tag||'kleopatra-daily-action',renotify:true,vibrate:[180,80,180],data:{url:d.url||'/kleopatra-app'},actions:[{action:'open',title:'Ajánlat megnyitása'}]}))});self.addEventListener('notificationclick',e=>{e.notification.close();e.waitUntil((async()=>{const url=e.notification.data?.url||'/kleopatra-app',windows=await clients.matchAll({type:'window',includeUncontrolled:true}),existing=windows.find(w=>new URL(w.url).pathname===url);if(existing){await existing.focus();return existing.navigate(url)}return clients.openWindow(url)})())});
